interface:
  display_name: "commit-push-pr 提交并推送 PR"
  short_description: "通过贡献者 fork 提交修复 PR 并安全归位"
  default_prompt: "Use $commit-push-pr to move my completed local fixes into a fresh fix branch, verify them, push through my public fork, open an upstream GitHub pull request, then safely return to main."
policy:
  allow_implicit_invocation: false
