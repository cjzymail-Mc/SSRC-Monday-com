interface:
  display_name: "sync-main 同步主线"
  short_description: "快速同步远端主线、保留本地修复，有冲突时主动合并并按影响验证"
  default_prompt: "Use $sync-main to synchronize main, preserve unfinished work, take the fast path when possible, and resolve conflicts with only the necessary checks."
policy:
  allow_implicit_invocation: false
