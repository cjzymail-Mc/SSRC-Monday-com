# AGENTS.md — Flowboard 仓库宪法

> 最后更新：2026-08-28
> 本文件只保留跨任务、跨会话、跨 workflow 都必须成立的规则。产品范围见当前 feature 的冻结主线，项目状态见 `STATE.md`。

## 1. 权威与施工自治

- 冲突时依次遵循：**用户最新明确指令 > 当前 feature 的冻结主线/技术契约 > 当前任务明确指定的计划或交接 > 其他设计、状态与历史材料**。
- 不预设默认 workflow。主 agent 可直接使用原生 Codex / Claude，也可按任务风险与复杂度自主选择计划、分工、并行或独立复核，并对范围、整合、验证和最终结论负责。
- double-workflow 仅在用户或当前任务明确指定时启用；`.copilot-*`、`temp.md`、`team-task.md` 及旧 workflow 报告不会因仍在仓库中而自动恢复权威。
- 改冻结范围、升级候选功能、修改本文件等治理契约，必须先获得用户明确拍板。流程选择不得绕过人工门或扩大授权。
- 调用受外部 profile 约束的专家时，必须先读取该 profile 的当前合同；不得复制旧合同或由主 agent 冒充专家/中央知识库。

## 2. 真实使用场景与复杂度边界

- **部署事实**：Flowboard 当前服务于公司内部小团队，只在公司局域网内使用；服务、SQLite 数据库、附件和备份均保存在用户当前电脑的本地磁盘。它不是公网、云端 SaaS、多租户或跨地域系统，agent 不得擅自按这些场景设计。
- **身份事实**：员工姓名直接作为用户名；当前不建设自助注册、完整用户管理、SSO 或统一身份平台。账户名、密码及其调整以用户当前明确要求为准；未经要求，不得把密码策略或账号体系升级成任务、发布阻断或产品范围。
- **默认技术口径**：优先采用满足真实规模的最简单本地方案，例如现有 Python 服务、CMD 启动和 SQLite。HTTPS 反向代理、Windows 服务守护、容器、云部署、分布式数据库、高可用、集中监控等均不是默认上线条件；只有用户明确要求，或已有证据表明当前真实规模下确有必要，并经用户拍板后，才可升级。
- **性能诊断口径**：出现卡顿时先检查请求频率、轮询/SSE、SQL、数据量、锁竞争和前端重绘等真实瓶颈，不得未经证据把问题归因于 CMD、本地运行或缺少企业级基础设施。
- **建议分级**：agent 必须区分“当前必需”和“未来可选”。可以简短提示可选风险，但不得把未请求的企业级加固扩成实施范围、发布阻断、机器配置或长篇运维工程。
- **安全底线不降级**：小团队、local 和局域网边界不改变服务端权限、数据可恢复、真实库授权和审计要求；这些底线仍按下一节执行。

## 3. 不可突破的边界

1. **权限在服务端执行**：前端隐藏入口不等于授权，不得靠放松权限换取表面完成。
2. **数据变更可追踪、可恢复**：删除优先软删除；schema 迁移先备份并验证恢复；不得绕过回收站、保留期或审计链。
3. **真实运行库只读优先**：`flowboard.db` 仅作只读检查；未经用户明确授权，不得对其迁移、purge、restore、重建或执行其他写操作。验证使用临时库、隔离目录或副本。
4. **不扩范围、不造假 PASS**：候选池和“本轮不做”不是任务；不得删改既有测试迁就错误实现、伪造往返产物，或用静态界面/实现同源断言冒充真实功能。
5. **保护既有成果**：不得损坏 legacy 数据，不得覆盖、回退或清理用户及其他 agent 的任务外改动；标记为冻结/归档的目录保持只读。
6. **外部与不可逆动作需授权**：真实库破坏性运维、git commit/push、发布、部署及机器级配置变更，均须当前指令明确授权；一次授权不自动延伸到后续动作。

## 4. 验收与停手机制

- 验证强度与风险相称；涉及 API、浏览器交互、迁移或往返文件时，使用真实路径与真实产物，并保留关键负例。文档、静态渲染和测试自述不能代替动态证据。
- 决策证据的保真度必须匹配问题类型：权限、数据语义、规则和并发等逻辑项可以用文字问卷收口；布局、可见性、信息密度、手势、视角记忆和整体手感等体验项，不得只凭 Markdown 问卷冻结，须先在代表性数据下通过可交互原型、隔离试用或等价动态证据确认。静态稿可验证结构与方向，但不能代替真实容量和连续操作体验。
- 同一人工验收回路内，纯展示、可逆且不改变结构或行为的微调可先走相关专项验证，并在本轮收尾集中跑全量回归；结构、交互、数据、API、权限、迁移或往返变更须按风险即时扩大验证。快速验证不得被用来降低当前阶段门的最终验收标准。
- **体验 Bug 默认批量验收**：用户通常会连续、分批反馈多个小型体验 Bug。主 agent 可默认累计相关修复，自主按改动关联性、风险和反馈节奏决定何时测试、测试频率以及采用专项验证还是集中回归；不必在每个低风险微调后重复启动完整测试，但必须保留必要的最低限度检查，并在合适的批次边界补齐动态验收。
- **跨日自动补测**：进入新的自然日或新会话继续修复 Bug 时，如上一批改动尚无可核验的集中回归 PASS 证据，主 agent 应在施工新 Bug 前主动补跑相称的遗留测试，无需等待用户提醒。已有可信 PASS 证据时不得无意义重复；权限、数据、API、迁移、删除语义及其他高风险改动仍按风险即时验证，不得借批量验收延后关键测试。
- 只有当前任务的全部范围与人工门均满足才可宣称完成；局部测试通过不得冒充 feature、Gate、发布或上线完成。
- 同一实现/测试失败连续 2 次、发现与冻结契约冲突或范围漂移、或完成任务需要新增授权时，停止扩写并向用户报告证据与待决项。

## 5. 信息分层

- `AGENTS.md`：仅存本仓库宪法；不记录当前 Gate、临时任务、角色接力、问卷模板或具体测试技巧。
- 冻结主线/技术契约：定义产品做什么、不做什么；`STATE.md`：记录项目当前事实；feature 文档：承载局部需求与实现合同；`mc-plan/`、`team-progress/`：保存凝固的决策与验收证据，不作默认控制面。
- memory 只承载可复用的经验与偏好，不能覆盖本文件或冻结契约，也不能成为硬规则的唯一来源。技术经验放 `.claude/memory/`，用户偏好放 `.claude/auto-memory/`；Codex 新提案先进入 `.codex/memory-inbox/` 按 schema 与人工审核流转，不直接冒充正式记忆。
- 过程材料按现有目录索引归类，不平铺污染根目录。新建顶级目录前须与用户对齐，并同步更新 `STATE.md` 的目录索引。

## 个人偏好与约定

- 用户喜欢简洁直接的回答，不喜欢冗长的"为什么"开头铺垫
- **`temp.md` 约定（跨 repo 通用，Claude 与 Codex 都适用）**：每个 repo 根目录有 `temp.md` 作为临时缓存文件。用户说「读temp」「写入temp」时，指 **repo 根目录的 `temp.md`**（即 `<repo>/temp.md`），**不是** `C:\Temp\` 文件夹
  - **“读temp”是强制三连动作：读取 → 立即写空 → 验证为空。** 读取成功后，下一项动作必须是清空同一个 `temp.md`（保留文件，只把内容写空）；在清空并验空完成前，禁止分析内容、调用其他工具或回复用户。
  - **验收标准**：清空后文件长度必须为 `0`；若清空或验空失败，立即停止后续处理并向用户报告，禁止带着已读内容继续工作。
  - 读取与清空应放在同一次工具调用中连续执行，避免“已读但稍后再清”的空窗。此规则用于防止上一轮缓存污染下一轮，不得以“稍后还要引用”为由延迟清空。
