# Gate 5 体验修复清单与技术交接（2026-08-25～2026-08-26）

> 文档日期：2026-08-26；最后追补：2026-08-27
> 适用范围：`feature-01-项目时间管理-仪表盘` 的 Gate 5 真实数据体验收口
> 当前状态：本文所列修复均已施工并通过对应专项自动验证，仍等待用户整合人工体验；不代表 Gate 5、发布或上线完成
> 统计口径：按可独立验收的体验问题归并。归档数据模型、v18 迁移、一次性批量归档等新增能力或运维动作不计入 Bug 数量
> 权威顺序：用户最新明确指令 > `feature-01-项目时间管理-仪表盘/mainline-feature01.md` §7.1 > `14-`/`15-` 最新覆盖 > 本文。本文是冷启动交接与证据索引，不替代冻结契约

## 1. 结论速览

- 原始两天清单共收口 **20 个体验问题**：2026-08-25 共 15 项，2026-08-26 共 5 项；另完成 AUD-01/AUD-02 两项主动巡检修复。
- 2026-08-26 深夜至 2026-08-27 的连续人工复测又归并出 **15 组体验纠偏**，详见 §10；【我的项目】、周/月视图和编辑器批处理属于同期新增/结构性演进，单列于 §11，不混入 Bug 数量。
- 问题集中在七类：壳层与入口、日期标尺与画布、全屏、项目列信息密度、缩放/拖拽、草稿/撤销状态、单双仪表盘及编辑器复用。
- 本轮没有放松服务端权限，没有修改 schema/API，没有写根 `flowboard.db`。
- 当前相关静态资源缓存版本：`timeline.css?v=20260827-r`、`timeline-ui.js?v=20260827-q`、`app.js?v=20260827-i`；壳层样式为 `styles.css?v=20260827-c`。
- 工作树尚未 commit/push；发布、部署和 Gate 5 人工终验均未获本轮授权。

## 2. 修复清单

### 2.1 2026-08-25：15 项

| # | 区域 | 原问题 | 修复后的行为 | 主要落点 |
|---|---|---|---|---|
| 1 | 日期标尺 | 部分缩放比例下日期、星期互相覆盖 | 按每日像素宽度自动选择“日号＋星期 / 日号 / 抽样月日” | `timeline-ui.js::dayScale/dayTicks/renderPortfolioScale` |
| 2 | 今天轴 | 红色今天线未覆盖日历顶部或被其他层遮挡 | 今天线从日历顶部贯穿项目行，并高于日历和蒙版 | `timeline.css` 的 today overlay/line 层级 |
| 3 | 今日蒙版 | 今天左侧白色蒙版 80% 过重 | 调整为 70% 白色；今天右侧保持原色 | `timeline.css::.timeline-past-mask` |
| 4 | 画布说明 | “完整数据 / 当前可见范围 / 操作提示”占用画布高度 | 三段说明整栏移除，不保留空白占位 | `timeline-ui.js::renderAll`、`timeline.css` |
| 5 | 季度层 | 年/季度/月/日四层信息过密 | 移除季度层，只保留年/月/日 | `timeline-ui.js::renderPortfolioScale`、`timeline.css::.timeline-e-quarters` |
| 6 | 单双一致性 | 单项目与全项目使用不同日期标尺 | 单项目复用全项目彩色、无碰撞年/月/日标尺 | `timeline-ui.js::renderDashboard/renderPortfolioScale` |
| 7 | 视角切换 | 缩放/中心点记忆造成切换项目或标签后视角异常 | 暂停视角记忆读写；每次切换恢复当前上下文完整范围 | `app.js::timelineViewportMemoryEnabled=false` |
| 8 | 全屏入口 | 全项目【全屏】入口被隐藏 | 恢复紧凑可见的【全屏】按钮 | `timeline-ui.js::renderAll`、`timeline.css` |
| 9 | 全屏切换 | 全屏内切换标签会退出全屏 | 切换上下文保持全屏，同时恢复该上下文完整范围 | `app.js` 的上下文切换、`timelineFilters.fullscreen` |
| 10 | 全屏布局 | 标签、退出按钮、画布和图例争抢空间 | 顶部横向标签＋右上退出；画布占剩余高度；图例固定底部 | `timeline.css::.timeline-portfolio-card.is-fullscreen` |
| 11 | 侧栏 IA | 侧栏项目分组逐项目直达造成冗余 | `#timelineProjectNav` 从 DOM 移除，中下部留白 | `index.html`、`styles.css`、`app.js` |
| 12 | 旧主页 | 旧 URL、返回按钮或空项目可能恢复经典仪表盘 | 经典 `#boardWorkspace` 保持不可达；时间管理落页接管 | `styles.css`、`app.js::landTimelineHome/showTimelineView` |
| 13 | 顶栏 | 旧项目导航 `Northstar Studio / Q3 产品路线图` 残留 | 隐藏旧导航，右侧工具保持对齐 | `index.html`、`styles.css` |
| 14 | 侧栏手感 | 收起按钮和文字动画粗糙 | 采用 248↔64px 中部悬浮轨、27×58px 胶囊和 280ms 动画，并保存用户本地收起偏好 | `styles.css`、`app.js` 侧栏状态 |
| 15 | 主页密度 | 团队项目与动态过长，raw/JSON 干扰阅读 | 每成员默认最多 10 个项目，可展开剩余项；动态只显示动作＋首条核心摘要 | `timeline-ui.js::renderHomeView/feedRows/feedRowSummary` |

### 2.2 2026-08-26：5 项

| # | 区域 | 原问题 | 修复后的行为 | 主要落点 |
|---|---|---|---|---|
| 16 | 侧栏按钮 | 项目增多或页面滚动后 `< / ›` 按钮向下漂移 | 按钮固定在浏览器视口 `50vh`，并随 248/205/64px 侧栏宽度贴边 | `styles.css::.sidebar-collapse` |
| 17 | 归档入口 | 全项目行和编辑器也出现【归档项目】 | 归档操作只保留在单项目仪表盘；全项目行和时间表编辑器不渲染 | `timeline-ui.js::projectTabs/renderAll` |
| 18 | 操作条初始态 | 鼠标经过项目即显示【撤销/放弃/更新】，遮挡项目内容 | 无草稿、无当前会话撤销时不生成操作条；形成草稿后主动显示，放弃后消失 | `timeline-ui.js::renderSingle/renderAll` |
| 19 | 操作条提交态 | 全项目点击【更新】后操作条永久常驻 | 提交成功后立即隐藏；现行方案由项目名称列右键打开共享菜单并显示红色【撤销】，hover 不再显示 | `app.js::acceptTimelineView/submitTimelineDashboardDraft`、`timeline-ui.js::portfolioUndoMenu/bindDashboard`、`timeline.css`；被 §10-R08 覆盖 |
| 20 | 滚轮命中 | 在左侧项目列滚轮会缩放时间轴 | 项目列和“项目”表头保留容器默认纵向滚动；只有右侧日期/轨道画布拦截 wheel 并缩放 | `timeline-ui.js::bindDashboard` |

## 3. 当前关键交互合同

### 3.1 全项目操作条状态机

```text
无草稿 + 无 lastBatchId
  └─ 不生成操作条；hover 不得凭空生成

形成日期/状态草稿
  └─ 仅显示【放弃 / 更新】
       ├─ 多项目：沿用项目行原位置
       ├─ 单项目：底部正中悬浮
       ├─ 放弃 → 恢复本地 original，回到“无草稿”
       └─ 更新成功 → 操作条消失，记录 lastBatchId

存在 lastBatchId
  └─ 项目名称列右键 → 显示红色【撤销】
       ├─ 不再由 hover 常驻显示
       └─ 撤销继续调用既有 batch undo API
```

实现要点：

- `editorState` 保存 `lastBatchId`；提交后的撤销入口由共享项目列右键菜单按目标项目解析。
- `renderPortfolioCanvas` 同时服务单项目和多项目；草稿动作根据 `singleProfile` 选择底部悬浮或行内呈现。
- 多个项目各自保存 `lastBatchId`，但页面只复用一份隐藏的项目撤销菜单，不生成多组常驻按钮。
- 撤销仍调用既有 batch undo API；本次只调整入口呈现，不改变审计、并发或权限合同。

### 3.2 全项目滚轮分区

```text
wheel target 位于 .timeline-portfolio-meta 或 .timeline-portfolio-axis-label
  └─ 不 preventDefault，不调用 setViewport → 浏览器纵向滚动项目容器

wheel target 位于右侧日期/轨道画布
  └─ preventDefault → 以鼠标位置为锚点调用 setViewport → 缩放时间轴
```

验收时必须同时检查：左列滚动后 `scrollTop > 0`，且 `data-viewport-days` 保持不变。

### 3.3 日期标尺密度

- `pixelPerDay >= 30px`：显示“日号＋中文星期”。
- `14px <= pixelPerDay < 30px`：只显示日号。
- `pixelPerDay < 14px`：按至少约 30px 间距抽样，显示“月/日”。
- 每个标签限制在自己的日期格内；季度层不渲染。

### 3.4 归档入口边界

- 【归档项目】：仅单项目仪表盘项目菜单。
- 全项目 active 行：无归档菜单。
- 时间表编辑器：无【归档项目】。
- 两个归档展示入口仍是全项目【已归档项目】和首页【已归档】；它们不是两个归档操作入口。
- 服务端 archive/unarchive 权限、一次确认、归档只读和审计链未改变。

### 3.5 侧栏定位

- 桌面展开宽度 248px；681～900px 展开宽度 205px；收起宽度 64px。
- 收起按钮使用 `position: fixed; top: 50vh`，水平中心贴合当前侧栏右边缘。
- 整个侧栏使用 `position: sticky; top: 0; height: 100dvh`，不随右侧内容下滚。
- 顶部账户/同步/退出区已移至侧栏底部；【设置】并入主菜单；桌面【经典仪表盘】入口和旧 74px 顶栏已删除。
- 全项目、本周、本月共享画布占满右侧一屏，浏览器页面不产生第二根纵向滚动条；项目容量超出时只滚动 `.timeline-portfolio-scroll`。

## 4. 自动验证证据

### 4.1 Node/UI

- 6 个 Node 测试文件曾在本轮整合回归全部通过：`view_ui`、`view_state`、`timeline_ui`、`schedule_ui`、`i12_ui`、`dashboard_ui`。
- 最后一次交互追补后再次运行 `node tests/timeline_ui.test.js`，全文件通过；新增断言覆盖：
  - 无草稿时单双仪表盘不生成操作条；
  - 草稿态主动显示；
  - 全项目提交后不生成常驻撤销动作；
  - 项目名称列复用一份右键撤销菜单，【撤销】具有红色 danger 状态；
  - 全项目无归档入口、单项目保留归档入口、编辑器无归档入口。
- 2026-08-27 追补后再次运行 `node tests/timeline_ui.test.js`，全文件通过；覆盖共享单双画布、周/月边界、取消缩放、拖动标签、项目列右键撤销、单项目悬浮草稿动作、编辑器 C 批处理、选中行后插入和草稿放弃恢复。

### 4.2 Chromium 专项

| 测试 | 覆盖 | 结果 |
|---|---|---|
| `test_gate5_sidebar_collapses_to_icons_and_persists` | 14 个项目压力数据；按钮固定视口中线；滚动前后不漂移；收起 64px 并持久化 | 1/1 OK |
| `test_timeline_v18_archive_mask_cluster_single_pan_and_fullscreen` | 全项目无归档菜单；进入单项目完成一次确认归档；归档视图、蒙版、密集点、横移与全屏往返 | 1/1 OK |
| `test_timeline_dashboards_share_context_controller` | 初始 hover 无操作条；草稿态仅【放弃/更新】；提交后隐藏；项目列 hover 不显示撤销，右键才显示共享红色【撤销】；单项目草稿动作固定底部；确认放弃后不重复弹窗 | 1/1 OK |
| `test_timeline_portfolio_e_d4_p1_canvas` | 项目列 wheel 使 `scrollTop` 增长，同时 `viewportDays` 不变；右侧画布仍可缩放 | 1/1 OK |
| `test_week_and_month_are_shared_portfolio_scopes` | 周/月视图复用共享画布；周范围锁定、月范围可向内放大 | 1/1 OK |
| `test_gate5_default_lands_timeline_home_when_projects_exist` | 顶栏从 DOM 删除；账户区在固定侧栏底部；26 项目时页面高度不超过视口且画布内部滚动 | 1/1 OK |

上述 Chromium 测试使用隔离临时库。没有把专项通过冒充 Python 全量回归或 Gate 5 人工终验。

### 4.3 静态检查

- 相关文件 `git diff --check` 通过；仅有仓库既有的 Windows LF→CRLF 提示，无空白错误。

## 5. 相关文件索引

| 文件 | 当前职责 |
|---|---|
| `AGENTS.md` | 体验决策必须使用代表性数据和动态证据；定义分层验证边界 |
| `feature-01-项目时间管理-仪表盘/mainline-feature01.md` §7.1 | 当前 as-built UI 最高产品真源 |
| `feature-01-项目时间管理-仪表盘/STATE.md` §0.12～§0.17 | 两天及后续追补施工事实、验证证据和等待人工门状态 |
| `feature-01-项目时间管理-仪表盘/14-PORTFOLIO_CANVAS_TECH_FREEZE.md` | 全项目组合画布合同及 08-25/08-26 覆盖 |
| `feature-01-项目时间管理-仪表盘/15-MANUAL_ARCHIVE_AND_TIMELINE_VIEW_TECH_FREEZE.md` | 归档、画布显示与最新入口/操作条覆盖 |
| `app.js` | 页面/全屏状态、周/月模式、草稿提交/放弃/离开恢复、项目级撤销与节点插入 |
| `timeline-ui.js` | 共享单双画布、日历密度、鼠标锚点缩放、拖拽、操作菜单和编辑器批处理 |
| `timeline.css` | 蒙版、今天轴、全屏、一屏画布、单双轨道列、悬浮动作与批处理菜单视觉规则 |
| `styles.css` | 顶栏移除、固定一屏侧栏、账户区下沉、经典仪表盘入口移除与低高度适配 |
| `index.html` | 壳层 DOM 与静态资源缓存版本 |
| `tests/timeline_ui.test.js` | 渲染层合同 |
| `tests/test_timeline_e2e.py` | 仪表盘真实浏览器交互 |
| `tests/test_gate5_home_e2e.py` | 主页与侧栏真实浏览器交互 |

## 6. 冷启动恢复步骤

1. 先读 `AGENTS.md`，确认真实库、授权、人工门和体验证据边界。
2. 读 `feature-01-项目时间管理-仪表盘/STATE.md` §0.12～§0.17，确认最新事实。
3. 读 `feature-01-项目时间管理-仪表盘/mainline-feature01.md` §7.1，作为当前 UI 执行真源。
4. 若处理全项目画布，再读 `14-PORTFOLIO_CANVAS_TECH_FREEZE.md` 最新覆盖；若处理归档/蒙版/入口，再读 `15-MANUAL_ARCHIVE_AND_TIMELINE_VIEW_TECH_FREEZE.md`。
5. 修改前先确认当前工作树；不得覆盖 `mc-plan/` 或其他用户/agent 的任务外改动。
6. UI 验证优先使用代表性的 25 active / 74 archived 隔离体验数据或测试临时库；未经新授权不得写根 `flowboard.db`。

## 7. 当前停手机制与下一步

- 原始 20 项、AUD-01/02 与 §10 的 15 组追补均属于 Gate 5 人工体验回路纠偏；§11 的新增视图/批处理能力单独记账。它们均未改变各并列 `WAIT_HUMAN` 状态。
- 当前安全下一步是用户按最新缓存版本进行整合人工体验；若仍有手感问题，继续按“代表性数据动态验证 → 小范围修复 → 对应专项 → 本轮收尾回归”处理。
- 同一实现或测试连续失败 2 次、发现与冻结契约冲突、需要真实库/commit/push/发布/部署授权时，立即停止扩写并报告。

## 8. 哪些问题依赖人工体验，哪些可由 agent 主动发现

> 分类标准不是“能否在修复后写自动化测试”，而是“在用户尚未表达偏好前，agent 能否仅凭现有合同和客观证据判定它是 Bug”。体验项经用户拍板后，同样可以转化为自动回归。

### 8.1 只能由人工体验拍板：7 项

这些问题没有唯一客观正确值。Agent 可以提供对照和测量，但不能代替用户判断“舒服、顺手、清爽”。

| 原编号 | 项目 | 为什么必须人工 |
|---|---|---|
| 3 | 蒙版 80%→70% | 正确透明度是视觉舒适度，不是逻辑真值 |
| 4 | 移除画布说明栏 | 说明是帮助还是噪音取决于实际熟练度和连续操作感受 |
| 5 | 移除季度层 | 信息价值与视觉负担的平衡属于密度偏好 |
| 7 | 暂停视角记忆 | “记住上次位置”与“每次看全局”都是合理产品策略 |
| 10 | 全屏布局重排 | 空间分配、标签高度和图例位置需要真实屏幕体验 |
| 14 | 侧栏动画与胶囊手感 | 动画时长、弹性、透明度和尺寸必须由人感知 |
| 15 | 首页项目/动态密度 | 默认 10 项、摘要长度和信息层级是团队阅读偏好 |

### 8.2 Agent 可自主排查并判定：7 项

这些问题具备可计算的不变量、既有明确合同或可重复的事件结果，不需要新增产品选择。

| 原编号 | 项目 | 可自动判据 |
|---|---|---|
| 1 | 日期标签重叠 | 相邻标签边界相交、文字越出自己的日期格 |
| 2 | 今天轴层级/高度错误 | 红线顶部坐标、底部坐标和 z-index 可测 |
| 9 | 全屏切标签退出 | 切换前后 `.is-fullscreen` 状态应保持 |
| 12 | 旧路径恢复经典主页 | 旧 URL、空项目、返回按钮均不得令旧工作区可见 |
| 13 | 旧顶栏残留 | 冻结为不可见的 DOM 在运行态仍可见即可判错 |
| 16 | 侧栏按钮随内容漂移 | 滚动前后按钮中心应恒等于 `innerHeight / 2` |
| 20 | 项目列滚轮误缩放 | 左列 wheel 后 `scrollTop` 应变化、`viewportDays` 应不变 |

### 8.3 人机协同：6 项

Agent 能发现不一致、遮挡或入口扩散，但在首次定方向时需要用户决定“保留哪一种行为”。用户拍板后，后续回归可完全自动化。

| 原编号 | 项目 | 人工与自动化的分工 |
|---|---|---|
| 6 | 单双标尺不一致 | Agent 找出差异；用户决定统一到哪一套视觉语言 |
| 8 | 是否恢复全屏入口 | Agent 能发现能力存在但入口隐藏；是否恢复由用户决定 |
| 11 | 是否移除项目分组直达 | Agent 能分析 IA 重复；入口取舍由用户决定 |
| 17 | 归档入口放在哪里 | Agent 能枚举入口和权限；操作路径由用户决定 |
| 18 | 草稿操作条何时出现 | Agent 能测遮挡和事件状态；显隐时机由用户决定 |
| 19 | 提交后收起、hover 再现、撤销标红 | Agent 能实现状态机；具体反馈节奏和危险色语义由用户决定 |

汇总：**人工体验拍板 7 项 + Agent 可自主判定 7 项 + 人机协同 6 项 = 20 项**。

## 9. 2026-08-26 Agent 主动巡检发现（AUD-01 已施工；AUD-02 后续被覆盖；AUD-03 待审核）

> 用户于 2026-08-26 明确指令“`AUD-01 - AUD-02 先修复`”，授权仅覆盖前两项；AUD-03 仍为候选项，不授权修改生产代码。
> 巡检方法：静态检查当前 DOM/CSS/事件状态机；使用 `TimelineProductionE2E` 同构的临时 v18 隔离库启动本地服务，在 Chromium 1280×800 与 640×800 视口执行测量。临时探针脚本执行后已删除，根 `flowboard.db` 未读写。

### AUD-01｜全项目滚轮缩放的鼠标锚点发生日期漂移

- **审核状态**：`IMPLEMENTED_AUTO_VERIFIED`；仍待 Gate 5 整体人工手感确认。
- **发现方式**：Agent 可自主判定；真实 Chromium 几何测量。
- **复现**：在全项目右侧日期画布约 78% 横向位置滚轮放大一次。
- **证据**：滚动容器左边缘 `x=273`，真实 `.timeline-e-axis` 左边缘 `x=607`，实际非时间区域宽度为 **334px**；当前 `bindDashboard` 固定使用 `leftWidth=220`。同一屏幕指针下的日期由 `2026-09-13` 变为 `2026-09-17`，漂移 **4 天**。
- **根因判断**：JS 只扣除了 220px 项目元数据列，没有扣除右侧轨道标签/内边距形成的额外 114px；wheel 锚点比例、可用宽度和实际时间绘图区不是同一坐标系。
- **影响**：用户把鼠标放在某日期附近放大时，该日期不能稳定留在指针下；连续缩放会产生“画布向一侧滑走”的手感。相同宽度误差也可能让日期密度估算偏乐观。
- **实际修复**：`timeline-ui.js` 由 `.timeline-e-axis` 与 chart 的实时 `getBoundingClientRect()` 计算左/右非时间区和有效时间宽度，wheel 事件发生时重新测量；缩放中心以当前已渲染 `data-viewport-center` 为真源，避免全屏/边界夹取后继续读取旧 filter center。
- **自动验收**：`test_timeline_portfolio_e_d4_p1_canvas` 在普通视图左/中/右三个锚点，以及全屏连续三档放大中逐轮比较指针日期，漂移均 `<=1 天`，1/1 OK。

### AUD-02｜提交后的红色【撤销】没有键盘可达路径

- **审核状态**：历史方案曾为 `IMPLEMENTED_AUTO_VERIFIED`，后被用户明确拍板的“项目名称列右键 → 红色撤销”覆盖；现行合同见 §3.1/§10-R08。
- **发现方式**：Agent 可自主判定；静态可访问性检查＋真实 Chromium Tab 巡检。
- **证据**：`is-undo-only` 默认 `display:none`，只由 `.timeline-portfolio-meta:hover` 或理论上的 `:focus-within` 显示；但项目 meta 没有 `tabindex`，排序把手为 `tabindex=-1`，隐藏的撤销按钮自身不能获得焦点。提交后移动鼠标离开，连续按 Tab 45 次均未到达 `[data-dashboard-undo]`。
- **影响**：只使用键盘、辅助技术或不能稳定 hover 的用户无法执行当前会话撤销；CSS 中的 `focus-within` 分支实际上不可触发。
- **历史修复**：曾以条件化 `tabindex=0`、`role=group` 和 `focus-within` 暴露撤销；该 hover/focus 展开形态现已移除。
- **当前实现**：提交后不显示常驻撤销；项目名称列触发 `contextmenu` 后，共享菜单显示红色【撤销】。这符合用户最新明确交互选择，但“纯键盘如何打开该右键菜单”仍应在后续可访问性验收中单独确认，不能继续沿用历史 1/1 结果宣称现行方案已键盘闭环。

### AUD-03｜≤680px 时所有顶级时间管理导航同时消失

- **审核状态**：`PENDING_HUMAN_REVIEW`，且需先确认窄屏是否属于 feature01 支持范围。
- **发现方式**：Agent 找到确定的可达性缺口；是否纳入本轮由用户拍板。
- **证据**：当前窄屏 CSS 虽保留底部账户区，但 `.sidebar>.main-nav` 仍为 `display:none`；`#timelineBtn`、`#timelineAllBtn` 均不可见，仍没有对应的移动端抽屉、汉堡按钮或底部导航替代。
- **影响**：窄屏用户即使已登录并落在某个时间管理页面，也无法通过顶级导航稳定切换【我的工作 / 全项目仪表盘 / 标签 / 已归档】。这不是视觉压缩问题，而是入口不可达。
- **范围判断**：主线仍把“响应式最终美化”列在集成验收范围，因此不能直接假定移动端永久不支持；但团队主要使用桌面浏览器，是否投入修复需用户确认。
- **建议修复方向（未执行）**：若支持窄屏，提供复用现有按钮权限/状态的移动导航或侧栏抽屉；若明确只支持桌面，应冻结最小支持宽度并给窄屏明确提示，而不是静默隐藏全部入口。
- **建议验收**：640px 下四个顶级时间管理入口至少有一个稳定、键盘可达的导航载体，切换不恢复经典主页且不丢未提交草稿保护。

### 9.4 审核入口

当前审核结果：

- `AUD-01：已授权并完成施工/自动验收，待人工手感复核`
- `AUD-02：历史键盘方案已被用户右键菜单方案覆盖；现行菜单待独立可访问性复核`
- `AUD-03：纳入窄屏支持并修 / 明确桌面优先暂不修 / 调整支持宽度`

在 AUD-03 收到明确审核前，不因该项修改 `app.js`、`timeline-ui.js`、`timeline.css`、`styles.css` 或测试合同。

## 10. 后续人工复测追补（2026-08-26 深夜～2026-08-27）

> 本节是对原 08-25～08-26 清单的连续验收追补，不回写历史编号 1～20。以下按可独立复测的 15 组体验问题归并；其中部分发生在 2026-08-27，因此明确标注追补日期，不伪装成原始两天内已完成。

| 编号 | 区域 | 复测问题 | 现行修复结果 | 主要落点/证据 |
|---|---|---|---|---|
| R01 | 多项目聚焦 | 双轨默认双行拥挤，放大项目的聚焦感不够 | 采用 B 方案；单击项目使行高扩大且同屏最多聚焦 1 个，切换项目自动收起上一个；并行轨始终保留细线 | `timeline-ui.js::renderProjectLanes`、`timelineFilters.focusedProjectId`、Node 聚焦断言 |
| R02 | 项目名称列 | 项目列整片白色、层级弱；常驻“当前/临近/逾期/本周”文字造成噪音；透明度异常 | 行块重新切割；当前/临近延迟 hover 1 秒显示；逾期/本周改为红点即时披露；修复项目区和时间线透明度，移除主线/并线常驻字样 | `timeline-ui.js::renderPortfolioProject`、`timeline.css` |
| R03 | 今日线/蒙版/测试色 | 今天左侧蒙版消失；【今天】随项目下翻；测试橙与延期红融合 | 恢复今天左侧白色蒙版并修正层级；【今天】固定于日历留白行；测试色与延期红拉开；单双画布复用相同处理 | `timeline-ui.js::renderPortfolioCanvas`、`timeline.css`、蒙版/今天线专项 |
| R04 | 日期边界 | 仅并行轨有节点或节点全在过去的项目，时间线越出白色蒙版/日期范围 | 日期边界统一同时采集主线和并行节点，并对过去项目夹取到共享日历范围；同类项目不再跑出蒙版 | `timeline-ui.js::bounds/dashboardRange`、`past-only parallel` Node/Chromium 回归 |
| R05 | 冗余标题/回退 | 单双仪表盘仍残留“绝对日历”、项目轨道、右上角返回箭头和说明文案，删除后还遗留空白行 | 删除所有页面右上角返回箭头及对应空行；删除绝对日历/项目轨道/操作说明，仅为【今天】保留必要日历空白行 | `index.html`、`timeline-ui.js::renderWorkspace/renderProjectShell`、Gate5 浏览器回归 |
| R06 | 缩放 | 滚轮缩放会把今天轴强制居中，画面漂移；缩放后无一键回到 100% | 以鼠标所在日期为锚点缩放；发生缩放才在【全屏】左侧显示红色【取消缩放】，100% 时隐藏；周视图禁用缩放 | `timeline-ui.js::bindDashboard`、`data-portfolio-cancel-zoom`、锚点漂移 `<=1 天` 专项 |
| R07 | 拖拽反馈 | 拖动节点同时出现两个黑色日期标签，且日期偏移难扫读 | 删除上方短日期标签；下方统一为 `YYYY-MM-DD \| ±N天`，偏移天数单独使用红色字体 | `timeline-ui.js` D4 `target-label/target-delta`、Node/UI 断言 |
| R08 | 草稿与撤销 | 更新后每个项目长期出现【撤销】，多个项目同时堆积操作按钮 | 草稿态只保留【放弃 / 更新】；提交后按钮消失；撤销改为项目名称列右键菜单中的红色选项，多项目共享一份菜单 | `portfolioUndoMenu/bindDashboard`、`acceptTimelineView`、Node 右键撤销合同 |
| R09 | 单双仪表盘漂移 | 单项目与多项目各自维护画布，外观和修复容易分叉 | 单项目改为 `renderPortfolioCanvas(..., profile:'single')`，多项目为同一核心的 `profile:'multi'`；日期标尺、节点、蒙版、缩放和菜单共用 | `timeline-ui.js::renderSingle/renderAll/renderPortfolioCanvas`、共享渲染测试 |
| R10 | 单项目轨道列 | 单项目左侧“项目轨道”冗余且主线/并行列过宽、易被日历图层覆盖 | 删除“项目轨道”；轨道列缩窄至原约 1/3，主线/并行文字居中并冻结，免受横向缩放和平移覆盖 | `timeline.css::.is-single-profile/.timeline-single-track-key`、单项目渲染断言 |
| R11 | 编辑器连续性 | 切到时间表编辑器后重复渲染项目标题/说明，头部跳变 | 单项目仪表盘与编辑器共用项目头部和页签，只切换下部内容；删除编辑器内部重复标题区 | `timeline-ui.js::renderProjectShell/renderEditor`、编辑器 shell 测试 |
| R12 | 编辑器工具条 | 次要按钮过多；保存/放弃/撤销长期同时显示；新增节点总插到末尾 | 删除轻量复盘、纠正初始日期、撤销上次提交和草稿计数等红圈项；保存按钮缩窄仅显示“保存”；人工修改后显示【放弃更改】，保存后切为【撤销】；选中节点后新增插在最后一个选中节点下方 | `renderEditor/addTimelineNode/orderedEditorNodes`、编辑器真实浏览器测试 |
| R13 | 单项目草稿菜单 | 单项目拖动日期或修改状态后，行内按钮破坏画布连续性 | 单项目【放弃 / 更新】改为页面底部正中悬浮；多项目明确保持原行内方式不变 | `timeline-single-draft-actions`、单双草稿动作 Node 断言 |
| R14 | 放弃离开循环 | 确认“放弃并离开”后草稿状态未恢复，导致同一提示反复弹出 | 用户确认后调用 `discardAllTimelineDrafts()`，按 `original` 重建所有脏草稿并同步编辑器状态；随后离开不再重复提示 | `app.js::allowTimelineLeave/discardAllTimelineDrafts`、取消保留/确认恢复/不重复浏览器回归 |
| R15 | 壳层与双滚动 | 74px 顶栏浪费空间；多项目需先滚主页再滚画布；侧栏随内容下移 | 删除桌面顶栏；同步/用户/退出移至左栏底部；设置并入主菜单并删除经典仪表盘入口；侧栏固定一屏；全项目/周/月画布占满右侧，26 项目时仅画布内部滚动 | `index.html`、`styles.css`、`timeline.css`；800px 高 Chromium：页面高度 `<= viewport+1`、内部 `scrollHeight>clientHeight` |

### 10.1 当前验证边界

- `node tests/timeline_ui.test.js`：追补后的共享单双画布、周/月范围、菜单、拖拽标签、编辑器批处理与草稿合同全文件通过。
- Chromium 专项：周/月共享画布 2/2 OK；26 项目壳层与内部滚动 1/1 OK；此前缩放、蒙版、密集点、草稿提交/撤销和编辑器旅程均使用隔离临时库验证。
- JavaScript `node --check`、Python `py_compile`、`git diff --check` 通过；仅有 Windows LF→CRLF 提示。
- 本节没有修改 schema/API/服务端权限，没有写真实 `flowboard.db`；未获得 commit/push/发布/部署授权。

## 11. 同期新增能力与结构演进（不计入 Bug 数量）

| 能力 | 已确认合同 | 复用/边界 |
|---|---|---|
| 系统【我的项目】标签 | 恒定在【全部】右侧第二位；不可编辑；自动包含当前成员创建且未归档项目 | `context_type:'mine'`；不是普通标签，不写共享标签表 |
| 【本周工作】 | 位于【我的工作】下的缩进子菜单；日历锁定本周，不可缩放；只显示本周有活跃节点的项目，规则优先于标签 | 与全项目共用 `renderAll/renderPortfolioCanvas`，仅传入 `timeScope:'week'` |
| 【本月工作】 | 同属【我的工作】子菜单；最大范围为自然月，可继续向内放大；仅显示本月有活跃节点项目，优先于标签 | 与全项目共用同一渲染源，`timeScope:'month'` |
| 编辑器 C 批处理 | 节点可多选/全选；字段下拉仅允许【项目阶段/轨道/状态】，值下拉随字段变化；红色【确认】；保留批量删除 | 批量修改先落本地 draft，仍由既有【保存】一次提交；删除沿用可恢复 batch/audit 合同 |
| 全/周/月项目排序 | 全项目保存后不得回跳；周/月可按活跃项目子集拖拽，但必须锚点式写回完整顺序并保持隐藏项目相对顺序 | 共享个人 order 真源；Pointer Events 不透明 D 浮层＋90ms 线性 FLIP；不建立周/月独立 order |
| 画布精准节点维护 | 鼠标扫过轨道显示虚线日期轴；空白处右键只输入名称即可插入；节点右键可编辑多行备注或红色删除 | 单双画布共用；项目/日期/轨道/阶段从命中位置推断；所有动作先落 draft，更新后才走 batch/审计/软删除 |

## 12. 当前人工门与未决项

- 本文记录“已施工并专项自动验证”，不代表 Gate 5 人工体验、发布或上线完成；各 `WAIT_HUMAN` 状态不提升。
- `AUD-03` 仍未拍板：窄屏顶部时间管理导航目前不可达；底部账户区的保留不等于已提供移动导航。
- 用户已拍板项目列右键撤销；其纯键盘/辅助技术打开路径尚未按现行方案重新闭环，后续不能引用旧 hover/focus 测试当作现行证据。
- 当前工作树包含连续多轮未提交改动；后续施工必须保护这些成果，不得回退、覆盖或以旧文档恢复已被最新指令取代的交互。
