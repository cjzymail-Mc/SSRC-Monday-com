# FINAL PLANNER PASS — Flowboard 全项目持续开发完成

## OUTCOME

PASS。`.copilot-task.md` 全局 done_when 1～17 已逐项完成并由 planner 独立复验；I3～I13、最终端到端验收、部署/恢复演练与文档收口均闭环。

## PRODUCT_SCOPE

- 面向约 15 人局域网自托管团队的看板与视图主线已完成。
- 看板/分组/任务、动态字段、查询/保存视图、表格/Kanban/日历、子任务/依赖、关系/镜像/公式、模板/导入导出、时间线/甘特、图表/跨看板仪表盘均共享统一数据、权限、version 与 activity 语义。
- 评论/提及/附件/订阅、通知/搜索、SSE + 轮询降级、审计、批量、完整详情、回收保留、受保护 purge、备份/恢复、安全和移动端必要配套已完成。
- 冻结排除项未被扩入：工作负载/报表导出、项目/OKR/工时/风险、表单、自动化/Webhook、CRM/Dev/Service、AI/Workdocs、外部集成、插件、SSO/LDAP、原生 App/完整离线、文件/地图视图均不在本轮。

## INDEPENDENT_FINAL_EVIDENCE

- `GLOBAL_ACCEPTANCE.md` 对全局 17 项、I3～I13/schema v3～v15、关键旅程、安全矩阵和 Windows 运维演练建立了当前证据索引。
- planner 最终执行 `python -m unittest discover -s tests -p "test_*.py" -v`：79/79 PASS，0 skip/expected failure，含 9 个真实 Chromium E2E。
- planner 对发现出的全部 5 个 `*.test.js` 执行 Node test：5/5 PASS，0 skip/todo。
- 全部 24 个 Python 文件 `py_compile`、全部 11 个 JavaScript 文件 `node --check`、`git diff --check`：PASS；仅有非错误 LF→CRLF 提示。
- 运行库只读证据：schema_migrations 精确为 1～15；每项 checksum=`flowboard-schema-vN`；`user_version=15`；integrity=`ok`；foreign_key_check=[]；关键计数保持 workspace 1 / board 1 / groups 2 / tasks 5 / attachments 0 / audit 2。
- I12 planner 独立发现并关闭实时有限批次 cursor 跳跃；5-event/limit=2 复现最终 5/5 无重无漏。
- I13 planner 独立发现并关闭 restore `after_db_old` 附件丢失；最终复现证明原 DB、附件目录与 blob 全保留，integrity/FK 正常且无 stage/old/lock 残留。
- 隔离运维证据覆盖备份 manifest/checksum、真实 DB+blob 恢复、损坏包拒绝、保留策略、8 点故障注入回滚、缺失目标语义和 pre-restore safety backup；未对运行库执行 purge/restore/rebuild。
- Windows CLI help、参数顺序、backup/list/verify/retention/restore 与 `WINDOWS_OPERATIONS.md` 一致；冷启动/临时端口/安全关停由真实 HTTP/E2E fixture 验证。

## DELIVERABLES

- `README.md`：当前功能、启动、验证与运维入口。
- `PHASE1_ARCHITECTURE.md`：schema v15、统一架构及生产边界。
- `WINDOWS_OPERATIONS.md`：Windows LAN 启动、升级、备份、恢复、回滚和安全边界。
- `GLOBAL_ACCEPTANCE.md`：全局 17 项最终证据索引。
- `subplan01.md`：I1～I13 与最终候选状态收口。
- `PROJECT_MAINLINE.md`：冻结主线保持未偏离。

## AUTONOMOUS_DECISIONS

- 保持渐进式模块化单体、SQLite、原生 Web；未引入微服务或重型平台。
- 实时采用 SQLite 持久 cursor + SSE + JSON 轮询降级，raw scan-window/last-scanned cursor。
- 批量采用全有或全无；restore 保持 offline-only；purge 使用预览/hash/精确确认/pre-purge backup/blob quarantine。
- 最终旅程采用多个真实、可定位的 E2E 组成完整覆盖矩阵，而非单个不透明超长脚本。

## PENDING_HUMAN_NONBLOCKING

- 无。

## OPEN_BLOCKERS

- 无。

## NOTE

未执行 Git commit/push/发布或机器级防火墙/服务配置。PowerShell 启动时 `.gitconfig/.npmrc` 权限输出为环境噪音，不影响产品与验收。
