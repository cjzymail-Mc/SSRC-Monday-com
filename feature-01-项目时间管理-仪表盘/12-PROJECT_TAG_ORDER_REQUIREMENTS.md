# feature01 · 共享项目标签与个人拖拽排序问卷

> 建立日期：2026-08-24  
> 状态：**Q1–Q14 已全部收口；用户选定【标签】顶级管理页方案 A“单标签双栏工作台”；v17 独立增量已完成正式实现与自动验收，当前为 `TAG_INCREMENT_WAIT_HUMAN`。**  
> 用户提案基线：约 6 个账号、25 个成员项目；【我的项目】允许每位用户保存互不影响的个人顺序；【全项目仪表盘】允许全员共同维护共享标签、一个项目可有多个标签，并允许每位用户保存互不影响的个人顺序。  
> 现有冻结约束：`mainline-feature01.md` §4.3 原只冻结“项目多选筛选 + 四种派生排序下拉”；本轮用户已显式选择新增共享标签、个人手动顺序及标签权限例外，待静态 HTML 选型确认后申请同步主线并进入技术方案。  
> 问卷节奏：原按最小决策单元逐轮追加；2026-08-24 用户明确要求剩余问题一次性问完，因此本版集中列出 Q2–Q13，便于同轮填写。

---

## 0. 本轮确认记录

1. **Q1 = A（用户拍板，2026-08-24）**：每个标签各有一套用户个人顺序；在一个标签中拖动，不改变该用户在其他标签或“全部”中的顺序，更不影响其他用户。  
2. **Q3 = A（mc-expert 陪审定稿，高）**：“全部”显示所有项目，另设系统虚拟入口“未分类”。  
3. **Q6 = A（mc-expert 陪审定稿，高）**：v1 不提供标签拖拽排序；系统入口固定在前，普通标签按名称自然排序。  
4. **Q9 = A（mc-expert 陪审定稿，高）**：新项目或首次出现的项目追加到个人顺序末尾。  
5. **Q10 = A（mc-expert 陪审定稿，高）**：拖拽只改个人顺序；共享标签归属通过独立的【编辑标签】入口修改。  
6. **Q11 = A（mc-expert 陪审定稿，高）**：标签按稳定 ID 隔离处理；改名保序，移除/删除不影响项目本体和其他标签。技术实现须保留审计并遵守软删除优先。  
7. **Q12 = A（mc-expert 陪审定稿，高）**：临时筛选或搜索生效时禁用拖拽，清除筛选后才能调整完整顺序。  
8. **Q13 = A（mc-expert 陪审定稿，高）**：项目重新加入标签视为新加入，追加到每位用户的该标签顺序末尾。  
9. **Q2 = A（用户拍板，2026-08-24）**：全项目大图一次只进入一个标签；同一项目不会因多标签在当前画面重复出现。  
10. **Q4 = A（用户拍板，2026-08-24）**：标签归属是团队共享元数据；所有成员都能给所有项目添加/移除标签，作为原项目写权限的显式、服务端可审计例外。  
11. **Q5 = A（用户拍板，2026-08-24）**：所有成员可创建、改名共享标签；仅 admin 可删除标签。  
12. **Q7 = A（用户拍板，2026-08-24）**：【我的项目】= 当前用户创建的项目，沿用 `created_by`，不新增负责人/参与人模型。  
13. **Q8 = A（用户拍板，2026-08-24）**：新增“自定义顺序”且作为默认；四种自动排序继续保留，自动模式禁用拖拽且不覆盖个人顺序。  
14. **新增页面要求（用户拍板，2026-08-24）**：首页左上角菜单在【我的工作】【全项目仪表盘】【已归档】【回收站】之外新增【标签】顶级页面；该页面向所有成员管理共享标签，可新增、改名，并通过拖拽仅显示项目名称的项目条目来添加/移除标签；先提供多个静态 HTML 方案供用户选型。  
15. **Q14 = A（用户拍板，2026-08-24）**：【标签】顶级管理页采用“单标签双栏工作台”——先选一个标签，再在“未加入 / 已加入”两栏之间横向拖动；静态基线为 `（标签管理静态稿）三案对照.html` 方案 A，像素细节仍留正式集成验收。  

> 陪审身份说明：以上均为 Codex custom agent 模拟 mc-expert，非 Claude 原生 mc-expert。

### 整体评估（2026-08-24）

- **无未填项、无题间冲突。** Q1–Q13 均有且只有一个选项被勾选。
- **权限覆盖已显式拍板。** Q4=A 只放开“项目 ↔ 标签归属”这一共享元数据，不放开项目名称、日期、节点、状态等内容权限；正式实现仍须服务端授权与审计。
- **两种拖拽的场景已隔离。** 全项目仪表盘内拖拽 = 当前用户在当前标签中的个人排序（Q10）；独立【标签】管理页内跨区域拖拽 = 修改所有人共享的标签归属（用户补充）。两者必须使用不同页面、提示和保存语义。
- **冻结排序未被删除。** Q8=A 是在原四种自动排序前新增默认“自定义顺序”；自动排序仅临时展示、禁用拖拽，不覆盖个人序列。
- **需求与 UX 已无待确认项。** 标签管理页已选方案 A；Q1–Q14 已同步进冻结主线与 v17 技术契约，实现和自动验收已完成，等待用户在隔离体验环境中确认真实使用感受。

---

### Q1 · 同一项目属于多个标签时，个人顺序如何生效（⚠️ 牵范围且影响核心体验，必须拍板）

**背景**：一个项目可以同时带有多个共享标签，例如“重点”“鞋类 A”“本月关注”。如果用户在“重点”标签下把项目拖到第一位，系统必须明确它在“鞋类 A”和“全部”中是否也跟着移动。这个选择决定排序记录是一套还是“每标签一套”，也决定用户是否会遇到“我只动了这里，为什么别处也变了”的困惑。

> 假设：标签归属是全局共享的；项目排序始终只对当前登录用户生效，任何选项都不会改变其他用户的顺序；【我的项目】保持一套独立个人顺序，不受本题影响。

**【你的回答 — 选一个】**

- [√] **A. 每个标签各有一套个人顺序（推荐）**：当前用户在“重点”里拖动，只改变自己在“重点”下的顺序；同一项目在“鞋类 A”和“全部”中的位置不变，其他用户也不变。最符合“每个标签下都能自由排序”，但同一用户需要维护多套顺序。
- [ ] B. 全项目仪表盘只有一套个人总顺序：当前用户无论在哪个标签里拖动，都会修改自己的全局项目顺序；其他标签只是按该总顺序筛出对应项目。规则简单，但无法在不同标签下表达不同的重要性。
- [ ] C. 只有“全部”允许拖拽：当前用户在“全部”里维护一套总顺序，各标签只筛选、不单独排序。实现和理解最简单，但不满足“每个标签下的项目都能自由排序”的原始设想。
- [ ] 其他想法：

> （补充说明，可选）：

#### mc-expert 陪审意见（只读陪审，不代替用户勾选）

- **建议**：A。共享的是标签与项目归属，个人化的是“当前用户在当前标签中的顺序”；在哪个标签拖动，就只改变该标签，最符合直接操作预期。
- **依据**：25 个项目的规模下，多套个人顺序仍可控；B 会产生“在重点标签拖一下，其他标签和全部也悄悄变化”的跨上下文副作用，C 则不满足“每个标签下都能自由排序”。
- **风险**：A 需要按“用户 × 标签”保存顺序，后续必须继续定义新项目加入、移除/删除标签、排序缺口、陈旧顺序与“全部”视图的处理；若未来改回单一总顺序，多套顺序无法无损合并。
- **与冻结主线的关系**：`mainline-feature01.md` §4.3 当前冻结的是四种派生排序下拉。新增手动个人排序属于明确增量；Q1 后还必须单独拍板“手动顺序与原四种排序下拉如何共存”。
- **置信度**：高。
- **升级结论**：本题影响核心体验且牵冻结范围，必须由用户拍板，不能按陪审意见自动定稿。

---

### Q2 · 标签在全项目大图中的展示方式【需人工拍板】

**背景**：同一项目可以属于多个标签。如果页面同时把所有标签展开成多个分组，同一项目会重复出现在大图中，25 个项目很快会变成长页面；如果一次只进入一个标签，则每个项目在当前画面最多出现一次，更接近 YouTube 播放列表，但不能同时纵览多个标签。

> 假设：全项目仪表盘继续保持“一张共享绝对日历大图”，标签只改变大图当前展示的项目集合，不把每个项目重新拆成独立卡片。

**【你的回答 — 选一个】**

- [√] **A. 一次只看一个标签（推荐）**：顶部或侧边显示标签列表，选择“全部 / 重点 / 鞋类 A”等任一标签后，大图只显示该标签项目；同一项目不会在一个画面重复出现。
- [ ] B. 所有标签同时分区展开：大图按标签分成多个区段；多标签项目会在不同区段重复出现，便于并排看标签，但页面更长、更拥挤。
- [ ] C. 标签可多选：用户同时选择多个标签，并可切换“满足任一标签（OR）/同时满足全部标签（AND）”；能力最强，但筛选和排序规则明显更复杂。
- [ ] 其他想法：

> （补充说明，可选）：

#### mc-expert 陪审意见（升级用户）

- **推荐 A，置信度高**：避免多标签项目在同一张绝对日历中重复出现，最适合约 25 个项目的阅读密度。
- **风险**：不能同时纵览多个标签；标签会成为进入当前项目集合的主导航。
- **升级原因**：决定全项目大图的核心信息结构，属于重大体验影响。

---

### Q3 · “全部”与无标签项目的含义

**背景**：你提出“项目不带任何标签时，默认标签就是【全部】”。常见产品中“全部”通常是一个不可删除的总入口，既包含有标签项目，也包含无标签项目；如果“全部”只代表无标签项目，用户会误以为这里能看到所有项目。

> 假设：“全部”和“未分类”均为系统虚拟入口，不是成员创建的普通标签，也不能改名或删除。

**【你的回答 — 选一个】**

- [√] **A. “全部”显示所有项目，另设“未分类”显示无标签项目（推荐）**：项目没有标签时仍出现在“全部”，同时出现在“未分类”；加上任意普通标签后，从“未分类”消失，但始终留在“全部”。
- [ ] B. “全部”显示所有项目，不另设“未分类”：无标签项目只通过“全部”找到，界面最简单，但项目多时不容易发现哪些尚未整理。
- [ ] C. “全部”就是无标签项目的默认标签：加上普通标签后项目不再属于“全部”。这最接近字面“默认标签”，但“全部”实际上不再代表全部项目。
- [ ] 其他想法：

> （补充说明，可选）：

---

### Q4 · 谁可以修改项目的标签归属（⚠️ 与现有项目写权限冲突，必须拍板）

**背景**：现有冻结权限是“admin 可改全部项目，member 只改自己创建的项目”；你的新提案又要求所有成员共同维护标签，并能修改项目标签。如果 member 可以给别人负责的项目加减标签，就需要把“标签归属”定义成团队共享元数据，作为现有项目内容写权限的一个明确例外。

> 假设：无论选择哪项，修改标签都不能顺带修改项目名称、日期、节点、状态等项目内容；服务端必须独立校验权限，不能只靠前端隐藏按钮。

**【你的回答 — 选一个】**

- [√] **A. 所有成员都能给所有项目添加或移除标签（推荐）**：标签归属属于团队共享整理信息；member 即使不是项目创建者，也能改该项目的标签，但不能改项目其他内容。
- [ ] B. member 只能改自己创建项目的标签，admin 可改全部：完全沿用现有项目写权限，但“所有成员共同维护”会受限。
- [ ] C. 所有成员都能添加标签，但只有项目创建者或 admin 能移除：降低他人误删分类的风险，但加与减的权限不对称，规则较难记。
- [ ] 其他想法：

> （补充说明，可选）：

#### 双方意见并列（升级用户）

- **Claude 原推荐 A**：你的最新提案明确强调“所有成员共同维护标签系统”，把标签归属视为团队共享整理信息，最贴合这次需求；代价是必须在服务端新增一个清晰、受审计的权限例外。
- **mc-expert 推荐 B，置信度高**：冻结主线目前是 member 只改自己创建的项目，B 能原样承接该权限；代价是其他成员不能替项目创建者整理标签，弱化“共同维护”。
- **升级原因**：A 会覆盖 `mainline-feature01.md` §2 的既有权限边界，并接近 §5 已排除的字段/子资源权限例外，必须由你显式决定。

---

### Q5 · 谁可以创建、改名和删除共享标签【需人工拍板】

**背景**：共享标签一旦改名或删除，会同时影响所有用户看到的项目分类；“共同维护”与“避免误改全团队结构”之间需要明确边界。删除标签只应删除分类关系，绝不能删除项目。

> 假设：所有成员至少都能查看全部标签；标签操作写入团队活动/审计；删除前必须二次确认，且项目数据本身永不随标签删除。

**【你的回答 — 选一个】**

- [√] **A. 所有成员可创建、改名；仅 admin 可删除（推荐）**：共同维护日常分类，同时把破坏性最大的删除留给 admin。
- [ ] B. 所有成员可创建、改名、删除：共同维护最彻底，但任何成员都可能改变全团队结构。
- [ ] C. 所有成员可创建；只有标签创建者或 admin 可改名、删除：责任边界更清楚，但标签创建者离职或不活跃时主要依赖 admin 接管。
- [ ] 其他想法：

> （补充说明，可选）：

#### mc-expert 陪审意见（升级用户）

- **推荐 A，置信度高**：全员创建/改名满足共同维护；删除影响全团队且更接近破坏性操作，保留给 admin。
- **风险**：member 改名仍会影响所有人，审计只能追踪、不能消除语义扰动；admin 也可能成为删除瓶颈。
- **升级原因**：决定共享结构治理与破坏性权限，影响全团队。

---

### Q6 · 标签列表本身是否允许排序

**背景**：项目可以在标签内拖拽，并不代表“重点 / 本月 / 鞋类 A”这些标签自身也必须拖拽排序。若标签顺序也能改，还需决定它是全团队共享还是每位用户独有，会额外增加一套排序规则。

> 假设：“全部”和“未分类”固定在最前；本题只讨论普通共享标签的显示顺序。

**【你的回答 — 选一个】**

- [√] **A. v1 不支持拖动标签，普通标签按名称自然排序（推荐）**：先把项目排序做好，标签顺序稳定、所有用户一致；以后确有需要再扩展。
- [ ] B. 每位用户拥有自己的标签顺序：不影响别人，但每个用户又多维护一套偏好。
- [ ] C. 标签顺序全团队共享，所有可编辑标签的成员都能拖动：共同维护，但一次拖动会改变所有人的界面。
- [ ] 其他想法：

> （补充说明，可选）：

---

### Q7 · 【我的项目】到底包含哪些项目（⚠️ 牵数据范围，必须拍板）

**背景**：你使用了“自己负责的项目”，而当前 feature01 已有的稳定归属字段是 `created_by`，权限也以“谁创建项目”为准；系统目前没有独立的“负责人/参与人”项目角色。若“负责”不等于“创建”，就需要新增负责人关系与维护入口，范围会明显扩大。

> 假设：admin 不因角色自动拥有“我的项目”；admin 只有在符合本题所选归属规则时，项目才进入其【我的项目】。

**【你的回答 — 选一个】**

- [√] **A. 【我的项目】= 我创建的项目（推荐）**：沿用现有 `created_by` 与权限模型；A/B/C/D/E 各自创建的 5 个项目就是各自的“我的项目”。
- [ ] B. 新增“项目负责人”，【我的项目】= 我负责的项目：创建者和负责人可以不同，需要新增字段、改负责人入口及权限关系。
- [ ] C. 新增多参与人关系，【我的项目】= 我参与的全部项目：一个项目可属于多人的“我的项目”，范围最大，也需要重新定义谁能编辑项目。
- [ ] 其他想法：

> （补充说明，可选）：

#### mc-expert 陪审意见（升级用户）

- **推荐 A，置信度高**：直接复用 `created_by`，与当前项目权限一致，不新增负责人或参与人模型。
- **风险**：现实中的负责人若不是创建者，“我的项目”语义会不准确。
- **升级原因**：你使用的是“负责”，现有数据只有“创建”；这决定页面的数据范围，不能代为解释。

---

### Q8 · 手动顺序如何与原有四种自动排序共存（⚠️ 与 mainline §4.3 冲突，必须拍板）

**背景**：`mainline-feature01.md` §4.3 已冻结“按开始日期 / 当前阶段 / 最近临近节点 / 逾期数”四种自动排序；现在新增拖拽后的个人顺序。如果自动排序仍在生效，用户刚拖好的位置会立即被算法重新排列，因此必须指定主次关系。

> 假设：自动排序只改变当前用户眼前的显示，不覆盖其已经保存的手动顺序；其他用户始终不受影响。

**【你的回答 — 选一个】**

- [√] **A. 增加“自定义顺序”并作为默认（推荐）**：自定义模式允许拖拽；切到四种自动排序时暂时禁用拖拽，切回自定义后恢复原个人顺序。
- [ ] B. 保留四种自动排序为默认，用户主动切到“自定义顺序”后才可拖拽：兼容旧习惯，但个人重要性排序不够突出。
- [ ] C. 移除四种自动排序，只保留拖拽自定义顺序：界面最简单，但会推翻已冻结的快速识别能力。
- [ ] 其他想法：

> （补充说明，可选）：

#### mc-expert 陪审意见（升级用户）

- **推荐 A，置信度高**：把拖拽诉求设为默认，同时完整保留四种已冻结自动排序；自动模式只临时展示，不覆盖个人顺序。
- **风险**：默认自定义可能弱化逾期/临近信号的主动暴露；自动模式禁用拖拽时必须给出清晰提示。
- **升级原因**：改变 `mainline-feature01.md` §4.3 的排序模型与默认体验，须显式拍板。

---

### Q9 · 新项目或首次看到的项目插入个人顺序的位置

**背景**：新建项目、刚被加进某标签的项目，或者老用户第一次打开一个标签时，都还没有该用户的排序记录。系统需要一个稳定的初始位置，且不能把用户已经排好的项目打乱。

> 假设：用户一旦手动拖动，之后就始终按保存的个人顺序显示；这里只决定“尚无个人位置”的项目如何加入。

**【你的回答 — 选一个】**

- [√] **A. 追加到末尾（推荐）**：不打乱既有顺序；新项目显示“新”提示，用户需要时再拖到前面。
- [ ] B. 插到最前：确保新项目最醒目，但频繁新增时会持续挤动用户原有顺序。
- [ ] C. 未排序项目先按开始日期插入：初始更有业务意义，但旧项目日期变化可能让“尚未拖过”的位置发生变化。
- [ ] 其他想法：

> （补充说明，可选）：

---

### Q10 · 项目拖拽是否同时修改共享标签

**背景**：拖拽可以只表示“调整当前标签内顺序”，也可以允许把项目拖到另一个标签来改变归属。后者更快捷，但一次看似个人化的拖动会改动所有人共享的标签数据，容易把“个人排序”和“团队分类”混为一谈。

> 假设：项目行提供清晰的拖拽手柄；标签归属始终另有可见入口可修改，不会只能依赖拖拽。

**【你的回答 — 选一个】**

- [√] **A. 拖拽只调整当前标签内的个人顺序（推荐）**：添加/移除标签通过项目的【编辑标签】菜单完成；个人动作与共享动作明确分开。
- [ ] B. 既能上下排序，也能拖到标签上添加归属：效率更高，但必须用明显反馈区分“个人排序”和“修改全团队标签”。
- [ ] C. 完整支持跨标签拖入/拖出：拖入添加、拖出移除标签；最像分组看板，但误操作共享数据的风险最高。
- [ ] 其他想法：

> （补充说明，可选）：由于【标签】属于全局共享，因此在首页的左上角，【我的工作】【全项目仪表盘】【已归档】【回收站】这几个菜单中，需要新增一个【标签】页面，在这个页面中，用户能够快速管理所有标签，可以新增标签、修改标签名称，并通过拖拽项目的方式，将项目快速添加进入某个标签、或者从标签中剔除某个项目。这里的项目仅显示项目名称即可，这个界面你帮我仔细设计下，给几种方案（静态html）给我选择

---

### Q11 · 标签改名、移除与删除后的数据处理

**背景**：排序绑定到标签时，如果标签改名、项目被移出标签或标签被删除，必须保证项目和其他标签中的顺序不会受牵连，也不能因为改名就丢掉个人排序。

> 假设：标签内部使用稳定 ID，名称只是可编辑显示文本；项目本体、节点、日期及其他标签关系都与当前标签的生命周期隔离。

**【你的回答 — 选一个】**

- [√] **A. 隔离处理（推荐）**：改名保留所有人的该标签排序；项目移出标签只清理其在该标签下的有效个人位置（审计另存）；删除标签只停用该标签关系及有效排序，项目本体不变，无其他标签的项目自动进入“未分类”。
- [ ] B. 标签改名后视为新标签，所有个人顺序重置；实现概念简单，但一次改名会让全员丢失排序。
- [ ] C. 删除标签时保留隐藏排序记录，未来创建同名标签自动恢复：看似可恢复，但同名不等于同一标签，容易恢复错误数据。
- [ ] 其他想法：

> （补充说明，可选）：

---

### Q12 · 临时筛选生效时是否允许拖拽排序

**背景**：标签先限定一组项目，现有“项目多选筛选”或未来的搜索又可能暂时隐藏其中一部分。如果只看见 3 个项目时拖动，系统很难直观表达它们与另外 7 个隐藏项目之间的新位置，容易出现筛选取消后顺序与预期不一致。

> 假设：标签与项目筛选按“先进入一个标签，再从该标签项目中继续筛选”的关系叠加；筛选只影响当前用户当前画面，不改变共享标签归属。

**【你的回答 — 选一个】**

- [√] **A. 临时筛选/搜索生效时禁用拖拽（推荐）**：仍可查看项目，但提示“清除筛选后可调整自定义顺序”；只有当前标签的完整项目列表中可以拖拽，规则最可预期。
- [ ] B. 允许拖动可见项目，隐藏项目保持原锚点：能力更灵活，但取消筛选后结果可能难以预判。
- [ ] C. 进入拖拽模式时自动清除临时筛选：保证操作对象完整，但用户当前筛选会突然消失。
- [ ] 其他想法：

> （补充说明，可选）：

---

### Q13 · 项目移出标签后再次加入，是否恢复旧位置

**背景**：项目可能被误移出标签，之后又重新加入。恢复旧位置有利于撤销误操作，但长期保留每位用户的旧位置会积累不可见数据；作为真正的新归类处理则更简单。

> 假设：通过即时撤销恢复刚发生的误操作属于实现层防错，不依赖永久保存旧位置；本题讨论隔了一段时间重新加入标签时的行为。

**【你的回答 — 选一个】**

- [√] **A. 视为新加入，追加到每位用户该标签顺序末尾（推荐）**：移出后清理旧位置；重新加入遵守 Q9 的“未排序项目追加末尾”，规则一致。
- [ ] B. 永久保留旧位置，重新加入时恢复：短期体验连续，但标签反复调整会长期积累隐藏排序记录。
- [ ] C. 30 天内恢复旧位置，超过后追加末尾：折中但引入额外时间规则和清理任务。
- [ ] 其他想法：

> （补充说明，可选）：

---

### Q14 · 【标签】顶级管理页结构选型【需人工拍板】

**背景**：Q1–Q13 已冻结标签、权限和排序规则；本题只选择【标签】页的主要工作方式。三案使用同一组 25 个假项目和同一套共享标签数据，可在 `（标签管理静态稿）三案对照.html` 顶部切换并实际拖拽体验。

> 假设：无论选哪案，项目条目都只显示项目名称；全员可新增/改名标签，仅 admin 可删除；标签管理页拖拽修改团队共享归属，全项目仪表盘拖拽只改个人顺序；颜色、字号、间距留正式集成验收微调。

**【你的回答 — 选一个】**

- [√] **A. 单标签双栏工作台（推荐）**：先选一个标签，再在“未加入 / 已加入”两栏之间横向拖动；动作对象和结果最清楚，适合 25 个项目的日常整理。
- [ ] B. 项目库 + 多标签分配板：左侧项目库，右侧多个标签列；适合连续把项目加入多个标签，但项目会跨列重复、页面更拥挤。
- [ ] C. 单项目标签检查台：先选一个项目，再集中核查和修改它的全部标签；单项目关系最清楚，但批量整理时切换次数最多。
- [ ] 其他想法：

> （补充说明，可选）：

#### mc-expert 陪审意见（升级用户）

- **推荐 A，置信度高**：一次只处理一个标签，拖拽来源与结果唯一，最容易与全项目仪表盘的个人排序拖拽区分，也最易实现和回滚。
- **风险**：无法像 B 一样同时纵览多个标签，也不如 C 适合逐项目核查全部归属。
- **升级原因**：这是新增顶级页面的核心信息结构，必须由用户看稿后选择。
