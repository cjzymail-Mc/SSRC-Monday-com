# feature01 · 仪表盘拖动调整日期需求讨论稿

> 建立日期：2026-08-13  
> 最后更新：2026-08-14  
> 状态：**Q1–Q22 全部确认（2026-08-14），拖动专题 + 权限模型（含技术契约）调研完成；mainline §2/§5/§9 已按用户签字落盘。** 未动代码，未定框架。剩余 UX 未决项见 feature01 `STATE.md` §7。
> 讨论对象：单项目仪表盘、全项目仪表盘中的节点拖动与延迟提交。  
> 当前视觉基线样图：`mockup.html`（已含可操作拖动草稿演示）；`drag-timeline-options.html` 为早期比较稿，已降级留档。

## 0. 本轮确认记录（2026-08-13）

用户对第一组关键问题全部选择 **A**，以下内容由“推荐/待确认”升级为本专题已确认需求：

1. 拖动某节点时，该节点与同轨道后续节点一起平移，后续各段间隔保持不变。
2. 节点不能拖到同轨前驱之前；拖到边界时停止，线性链顺序不变。
3. 点击【更新日期】前允许连续拖动多个节点；同一节点重复调整时以最终草稿日期为准。
4. 全项目仪表盘允许累计多个项目的草稿；【更新日期】一次提交当前页面全部待更新项目。
5. 批量提交只允许全成或全败：任一项目发生并发冲突时，整批不写入，全部草稿保留并提示冲突。
6. 当前视觉方向选择 **方案 A：极简顶栏**。拖动后在右上角显示醒目的红色【更新日期】，并同时提供待更新数量与【放弃更改】；不默认展开横向变更带或审阅侧栏。

以上决定仍服从用户最新明确指令；本文件其余问题继续调研。

### 第二组确认

用户对第二组问题也全部选择 **A**，新增确认如下：

7. 存在待更新草稿时，允许切换项目、筛选条件以及单项目/全项目仪表盘；草稿继续保留。
8. 存在草稿时刷新、关闭或离开网页，必须拦截并提醒；用户可以留在当前页面，或者确认放弃草稿。未经点击【更新日期】，任何离开动作都不得把草稿写回原始数据。
9. 拖动默认按 **1 个自然日**吸附，拖动过程中持续显示“旧日期 → 新日期（±N 天）”。
10. 已完成的历史节点仍允许在仪表盘中拖动，但必须明确标记为【历史日期修正】。
11. 点击红色【更新日期】后直接提交，不再默认增加第二层确认弹窗。
12. 提交成功后提供一次短时撤销；撤销作为新的反向变更批次写入审计，不删除或篡改原提交记录。

### 视觉基线补充确认

用户进一步明确：拖动功能不得另起一套仪表盘视觉，必须以既有 `mockup.html` 中已经敲定的视觉效果为基线继续迭代。独立的 `drag-timeline-options.html` 仅保留为早期交互比较稿，不再作为视觉方向来源。

当前 `mockup.html` 已加入可操作的拖动草稿演示，保留既有连续彩带、六阶段配色、绝对日历标尺、红色今日线、白色里程碑点和 2px 小圆角。新增反馈应克制地附着于该视觉体系：

- 空心旧位置点与虚线表示尚未提交的移动距离；
- 直接拖动节点使用浅蓝轮廓，自动顺延节点使用中性轮廓；
- 红色集中用于右上角【更新日期】按钮，不把整条草稿时间轴改成红色；
- 点击【更新日期】前仅修改样图草稿，点击后才模拟同步原始数据。

用户随后确认“OK，继续在这一版上收敛”。因此，当前 `mockup.html` 的整体视觉与拖动原型成为后续讨论基线；该确认表示继续迭代方向成立，不代表本文所有尚未回答的细节已自动冻结。

### 第三组确认（2026-08-14）

Q7、Q12–Q19 已全部填写，确认如下（13.～18. 为拖动细节，19.～21. 为权限与状态模型）：

13. 拖动与点击用**位移阈值区分**：按下移动超过 4px 才算拖动；单击打开详情（Q7）。
14. 筛选隐藏了带草稿的项目时，草稿保留并在顶部提示「另有 N 个项目 / N 个节点未显示」（Q12）。
15. 连续拖动时，空心旧位置点**始终钉在最近一次已提交位置**，虚线 = 相对原始数据的净位移（Q13）。
16. 提交后撤销**有效至页面刷新或下一次编辑开始**，不设固定倒计时（Q14）。
17. 跨项目批量撤销与提交/冲突三段对称：**整批回滚**，审计记一条反向批次（Q15）。
18. **权限基线升级（⚠️ 已与 mainline §2/§5 冲突，用户拍板选升级）**：纳入项目级创建者写权限——workspace admin 可改/拖动所有项目；member 只能改/拖动自己创建的项目（created_by）（Q16 选 A）。
19. **角色对齐**：管理员 = workspace admin（当前 1 名，够用），普通用户 = workspace member；不另建角色体系（Q19 两点均 A）。
20. **查看范围**：单项目/全项目仪表盘与编辑器查看对所有注册成员（admin + member）开放；当前团队只有 admin/member 两种角色、暂不引入 viewer；未登录不可见、不引入访客/公开分享机制（Q18 选 B + 批注）。mainline §2 原「非注册用户只读」与「未登录 401」现状悬空的表述随之作废。
21. **状态列**：三态枚举 `未开始 / 进行中 / 已完成`——节点日期 ≥ 今天 = 未开始；日期 < 今天且未手工标完成 = 进行中（隐含逾期）；手工标完成 = 已完成。**不单开完成日期列**，完成时间由审计/修改历史承载（Q17 两点均 A）。

> 18.～20. 的 mainline §2/§5/§9 同步改写文案已拟好，**待用户签字后落盘**（改冻结主线不代签）。
> 连带技术契约已升级为正式问卷题 **Q20–Q22**（created_by 不可变性 / initial_date 纠正权归属 / Excel 导入 created_by 归属），待用户填写后一并冻结。

### 第四组确认（2026-08-14 · 权限技术契约 + mainline 签字）

Q20–Q22 已填写，且用户签字同意 mainline §2/§5/§9 权限段落改写落盘：

22. **created_by 完全不可变**：首次创建系统写入后任何人（含 admin）不能改；建错人只能删了重建（Q20 选 A）。
23. **initial_date 例外纠正权仅限 workspace admin**：member 录错须找 admin，纠正留审计（Q21 选 B）。
24. **Excel 导入的项目 created_by = 导入操作者**：谁点导入算谁建；第一版不做「指定创建者」功能（Q22 选 A）。

mainline 已同步落盘：§2 整段替换（角色对齐 + 查看范围 + 创建者写权限 + 三条技术契约）、§5「项目级细粒度权限」精确化为字段级/部门级/项目级角色/ACL 仍不做、§9 完成边界同步新权限验收标准、头部加 2026-08-14 升级注记。

**至此 Q1–Q22 全部收口，本问卷（拖动专题 + 权限模型）调研完成。**

## 1. 用户已明确的需求

以下内容来自用户最新指令，可作为本专题的确认起点：

1. 【单项目仪表盘】和【全项目仪表盘】都允许用鼠标拖动任意节点。
2. 拖动用于精确调整项目时间轴上的节点日期。
3. 拖动发生后，页面右上角出现醒目的红色【更新日期】按钮。
4. 拖动本身只产生待提交的日期草稿，不同步改写【原始数据】。
5. 只有用户主动单击【更新日期】后，原始数据才同步修改。
6. 用户未单击【更新日期】时，原始数据必须保持不变。

## 2. 与既有规则的兼容解释（待用户复核）

拖动节点等价于在时间表编辑器中“直接修改该节点日期”，因此暂按已有规则解释：

- 横向拖动按自然日调整日期，不修改阶段、轨道、状态或备注。
- 拖动某节点时，该节点及其**同轨道后续节点**整体平移，后续各段原间隔保持不变。
- 主线与并行互不顺延、互不计算间隔。
- 拖动后，仪表盘立即展示草稿时间轴，同时保留原位置参照。
- 【更新日期】应把一次拖动产生的直接修改与自动顺延记录为同一操作批次。
- 正式提交后只修改当前 `date`；节点的 `initial_date` 不随拖动改变。
- 非注册用户继续只读，不出现可拖动态和【更新日期】按钮。

如果以上任一点不符合实际业务，应在冻结本专题前改正。

## 3. 推荐的交互状态

```text
已提交
  ↓ 开始拖动
拖动中（显示目标日期与 ±N 天）
  ↓ 松开鼠标
有待更新草稿（右上角出现红色【更新日期】）
  ├─ 继续拖动：累计草稿
  ├─ 撤销/放弃：回到最近一次已提交状态，不改原始数据
  └─ 更新日期：校验并批量提交
                  ├─ 成功：刷新原始数据、清除草稿
                  └─ 冲突/失败：保留草稿并给出可处理原因
```

“草稿”只表示当前浏览器页面中的待提交日期，不应写入正式活动记录；正式点击【更新日期】成功后才产生审计记录。

## 4. 样图中的三种视觉候选

### 方案 A：极简顶栏

- 右上角直接出现红色【更新日期】。
- 同时显示“有 N 个节点待更新”和【放弃更改】。
- 时间轴用空心原位置点 + 实心草稿点表达移动。
- 优点：干净、学习成本低。
- 风险：一次影响多个项目/节点时，提交范围不够直观。

### 方案 B：醒目变更带

- 顶部出现浅红色待更新状态带，红色【更新日期】仍位于最右侧。
- 状态带展示直接拖动数、自动顺延数和受影响项目数。
- 优点：很难误以为已经保存，适合全项目批量调整。
- 风险：会占用更多纵向空间。

### 方案 C：审阅侧栏

- 右上角显示带数量的红色【更新日期】。
- 拖动后同时打开待更新侧栏，逐项列出旧日期、新日期和变动天数。
- 优点：提交边界最清楚，适合高风险的跨项目修改。
- 风险：操作偏重，可能影响连续拖动效率。

## 5. 请用户继续确认

请直接在回复中按题号回答；也可以只先回答最关键的 1～4。

### Q1 · 顺延范围

> **已确认：选择 A。**

拖动节点 B 时，是否继续沿用“B 与同轨后续节点全部平移”的既有规则？

- **推荐：是。** 仪表盘与表格编辑器共享同一日期规则。
- 备选：只移动 B，后续节点不动并重算相邻间隔。

### Q2 · 全项目提交范围

> **已确认：选择 A。**

在全项目仪表盘连续拖动多个项目后，右上角【更新日期】应提交什么？

- **推荐：一次提交当前页面全部待更新草稿**，按钮显示“更新日期（N）”，提交前可查看受影响项目。
- 备选：每个项目分别提交，避免跨项目批量提交。

### Q3 · 多次拖动

> **已确认：选择 A。**

点击【更新日期】前，是否允许连续拖动多个节点、最后一次性提交？

- **推荐：允许。** 同一节点反复拖动只保留最终草稿日期。
- 备选：每次只允许存在一个直接拖动节点。

### Q4 · 离开页面

> **已确认：选择 A。切换视图或筛选时保留草稿；刷新、关闭或离开网页时拦截提醒。**

存在待更新草稿时，用户切换仪表盘、切换筛选项目、刷新或关闭页面，应如何处理？

- **推荐：拦截并提示“更新日期 / 放弃更改 / 留在当前页”。**
- 备选：静默放弃草稿；原始数据仍不变。

### Q5 · 拖动精度

> **已确认：选择 A，默认按 1 个自然日吸附。**

节点日期为单日里程碑，是否采用以下精度？

- **推荐：默认按 1 个自然日吸附**；拖动浮层持续显示“旧日期 → 新日期（±N 天）”。
- 备选：默认按周吸附，按住辅助键时再按天精调。

### Q6 · 已完成节点

> **已确认：选择 A，允许拖动并标记为历史日期修正。**

“任意节点”是否包括状态为【已完成】且日期早于今天的历史节点？

- 推荐：允许拖动，但提交前明显提示这是历史日期修正。
- 备选：已完成节点锁定，只能回到原始数据表修改。

### Q7 · 拖动与点击的区分

**背景**：节点除了能拖动改日期，将来还可能承担「点击查看节点详情」的动作。如果同一个节点既响应拖动又响应点击，快速点击容易被误判成一次微小拖动，反过来想拖的人轻轻一点也可能误开详情。需要先约定怎么区分这两种意图。

> 假设：日常高频操作是拖动调日期，查看详情是低频动作；误拖比误开详情更影响效率。

**【你的回答 — 选一个】**

- [√] **A. 位移阈值（推荐）**：按下后移动超过 4px 才算拖动；按下没移动就松开算单击，打开详情。节点整体可拖，不用画额外手柄。
- [ ] B. 独立拖动手柄：节点本体单击即开详情，只有按住节点旁的小手柄才能拖。误操作最低，但每次拖动都要精确抓小手柄。
- [ ] C. 不设阈值：节点本体可拖 + 单击开详情，靠浏览器原生 click/drag 判定。实现最简单，但快速点击容易被吃掉。
- [ ] 其他想法：

> （补充说明，可选）：假设一共5个用户，管理员可以任意修改（拖动）所有项目时间轴；普通用户只能编辑自己创建的项目时间轴；但项目仪表盘的查看功能对所有人开放。


### Q8 · 更新前审阅强度

> **已确认：选择 A（极简顶栏）。**

你倾向哪个视觉方案？

- A：极简顶栏。
- B：醒目变更带。
- C：审阅侧栏。
- 也可组合，例如“日常用 B；涉及多个项目时自动升级为 C”。

### Q9 · 提交后的撤销

> **已确认：选择 A，提供短时一次撤销，并记录反向审计批次。**

点击【更新日期】并成功写回后，是否仍提供一次“撤销本次更新”？

- **推荐：提供短时一次撤销**，并作为新的反向审计批次记录。
- 备选：提交后不可一键撤销，只能再次拖回原日期。

### Q10 · 并发冲突

> **已确认：选择 A。任一冲突导致整批不写入。**

如果拖动期间别的用户已经修改了同一项目日期，是否接受以下处理？

- **推荐：全批拒绝覆盖，原始数据一项都不写；保留本地草稿，展示服务端最新日期并让用户重新确认。**
- 备选：无冲突项目先写回，冲突项目继续保留草稿。
- 不推荐：当前用户直接覆盖服务端最新日期。

### Q11 · 能否拖过同轨前驱

> **已确认：选择 A，不允许跨越前驱。**

例如同轨 A → B → C，是否允许把 B 拖到 A 的日期之前，从而改变节点在线性链中的顺序？

- **推荐：不允许跨越前驱。** 拖到边界时停在前驱日期，链顺序保持不变；当前样图暂按此规则演示。
- 备选：允许跨越，并把节点顺序一并调整；这需要重新定义前驱、间隔和同日排序。

### Q12 · 隐藏范围中的草稿

**背景**：Q4 已确认「切换视图/筛选时保留草稿」。但在全项目仪表盘上，用户先拖出了几个项目的草稿，随后又用筛选条件把其中一些项目隐藏了——这些草稿在画面里看不见了，可它们仍属于待提交。这时该让用户怎么感知到「有看不见的草稿还在」。

> 假设：用户不会因为筛选而真的想丢掉草稿；只要提交前提醒到位，隐藏是可接受的。

**【你的回答 — 选一个】**

- [√] **A. 保留 + 顶部提示（推荐）**：草稿照常保留，顶部明确提示「另有 N 个项目 / N 个节点未显示」；提交时这些隐藏草稿仍计入【更新日期（N）】。
- [ ] B. 禁止切换到会隐藏草稿的筛选：强制先提交或放弃，才能换筛选。最安全，但打断筛选操作。
- [ ] C. 切换时弹窗三选一：「保留并隐藏 / 现在提交 / 放弃这些草稿」。最显式，但多一次打断。
- [ ] 其他想法：

> （补充说明，可选）：选 A 即顺接 Q4（筛选保留草稿），只是补「看不见的草稿」该怎么提示。


### Q13 · 连续拖动时的旧位置参照

**背景**：§0 第 3 条已确认「同一节点重复调整以最终草稿为准」。但视觉上每个被拖动的节点都有一个空心旧位置点 + 虚线表示移动距离。如果一个节点被连续拖了好几次（还没点【更新日期】），那个「旧位置点」到底该钉在哪里——是钉在最初提交的位置，还是钉在上一次拖完的位置？

> 假设：用户更关心「这次相对原始数据改了多少」，而不是「刚才那一下微调了多少」。

**【你的回答 — 选一个】**

- [√] **A. 始终显示最近一次已提交位置（推荐）**：空心点钉在原始数据位置，虚线长度 = 草稿相对原始数据的净位移；多次拖动的中间过程不累积视觉噪音。
- [ ] B. 显示上一次草稿位置：每拖一次虚线刷新，能看出本次微调量，但旧位置点会随每次拖动「漂」。
- [ ] C. 不显示旧位置点：只显示当前草稿点，最干净，但丢失「改了多少」的参照。
- [ ] 其他想法：

> （补充说明，可选）：


### Q14 · 撤销的时效与边界

**背景**：Q9 已确认「提交成功后提供一次短时撤销，并记录反向审计批次」。但「短时」到底是多久、什么时候这个撤销入口会消失，还没定。这决定撤销提示要显示多久，以及「提交后立刻刷新页面/开始下一次编辑」会不会让撤销失效。

> 假设：撤销主要防「手滑提交错了」，这种发现通常在提交后几秒内；不需要把撤销保留很久。

**【你的回答 — 选一个】**

- [√] **A. 有效至页面刷新或下一次编辑开始（推荐）**：提交后显示撤销提示，不设固定倒计时；一旦刷新页面或开始新的拖动，撤销入口消失。用户不被倒计时催促。
- [ ] B. 固定时间窗：如提交后 10 秒倒计时，过期撤销消失。边界明确，但用户分心时可能正好错过。
- [ ] C. 一直保留到下次提交：撤销入口不随时间消失，直到下一次点【更新日期】。最宽松，但「很久以前的提交还能一键反」在审计上语义模糊。
- [ ] 其他想法：

> （补充说明，可选）：


### Q15 · 跨项目批量撤销的粒度

**背景**：Q2 已确认全项目仪表盘「一次提交当前页面全部待更新草稿」，Q10 已确认并发冲突「整批拒绝、一项都不写」。提交是整批的、冲突也是整批的，那提交成功后的撤销——如果一个项目可能同时提交了跑鞋、篮球鞋、羽毛球鞋三个项目的草稿，撤销时是三个一起退，还是能只退其中一个？

> 假设：撤销应当和提交/冲突「三段对称」——提交整批、冲突整批、撤销也整批，审计上是一对正/反批次，最不容易出错。

**【你的回答 — 选一个】**

- [√] **A. 整批回滚（推荐，与提交对称）**：一次撤销把本次提交的全部项目一起退回，审计上记一条反向批次。和 Q2/Q10 的「整批」语义对称。
- [ ] B. 按项目逐个撤销：可以只退回其中某几个项目。灵活，但会拆散审计批次，且和 Q10 的「整批拒绝」不对称。
- [ ] C. 只回滚直接拖动的节点：被自动顺延的节点不回滚。不推荐——会破坏顺延链。
- [ ] 其他想法：

> （补充说明，可选）：


### Q16 · 拖动/编辑的权限粒度（⚠️ 与冻结主线冲突，必须拍板）

**背景**：你在 Q7 的补充说明里写了一条权限需求——

> 「假设一共 5 个用户，管理员可以任意修改（拖动）所有项目时间轴；普通用户只能编辑自己创建的项目时间轴；但项目仪表盘的查看功能对所有人开放。」

这条和**已冻结的 `mainline-feature01.md §2` / feature01 `STATE.md §5` 直接打架**。冻结主线现在的权限基线是：

> 「注册用户拥有项目时间节点的新增、查看、修改和删除权限；非注册用户只读；**本 feature 不做按项目、部门或字段进一步细分的权限模型**。」（mainline §2）
> 「明确不做：项目级细粒度权限。」（mainline §5）

也就是说，"普通用户只能改自己创建的项目"=**项目级写权限**，正是 mainline 明确排除的。这不是小修，是权限基线的反转，必须先定方向，否则数据模型和 API 会设计错。

**【你的回答 — 选一个】**

- [√] **A. 升级主线：纳入项目级写权限**：管理员可改全部；普通注册用户只能改「自己创建的项目」；查看对所有人开放。如实把 mainline §2/§5 的权限段落改成这个模型（要同步改 mainline，属于主线调整）。
- [ ] **B. 维持主线：注册用户全可改**：所有注册用户都能拖动/编辑任意项目，不分创建者；非注册只读。Q7 那段"只能改自己项目"的补充作废或降为「未来候选」。
- [ ] C. 折中：注册用户全可改，但留一个「项目创建者」标记，仅用于仪表盘展示（谁建的），不构成写权限限制。
- [ ] 其他想法：

> （补充说明，可选）：注意——无论选哪个，**权限必须由服务端执行**（mainline §2 已定），前端只隐藏按钮不算授权。选 A 还会牵连 Q10 的并发冲突：如果普通用户改不了别人的项目，那"别人同时改了同一项目"的冲突场景会减少，但管理员仍可能和别人撞车。


### Q17 · 状态列的枚举与完成日期（空白点，需从零定）

**背景**：编辑器源数据表有一列【状态】（`项目阶段｜轨道｜项目节点｜日期｜时间间隔｜状态｜备注`），但**这一列到底有哪些值、怎么来，至今没定**。`mainline-feature01.md §8` 把它列为"尚未冻结的设计细节"。PHASE2 里你之前对"状态表示什么"勾过「其他/还没想好」。现在需要把它收敛掉，因为它决定节点在仪表盘上怎么画、Q6 的"已完成历史节点"怎么识别、以及轻量复盘怎么算延期。

> 假设：状态值要少而稳，能由日期自动推断的尽量自动，减少手工维护。

**【你的回答 — 可多选 / 可自由改枚举】**

- [√] **A. 状态枚举（推荐三态）**：`未开始` / `进行中` / `已完成`。节点日期 ≥ 今天 = 未开始；节点日期 < 今天 且未手工标完成 = 进行中（隐含逾期）；手工标完成 = 已完成。
- [ ] B. 状态枚举（四态，把延期拆出来）：`未开始` / `进行中` / `已完成` / `延期`。延期由系统自动算（今天过了还没完成）。
- [ ] C. 只要两态：`未完成` / `已完成`，其余全靠日期推断，最简。
- [ ] 其他枚举（写你想要的）：

**再单独确认一点 ——「已完成」需不需要单独的完成日期？**

- [√] **A. 不需要（推荐）**：状态列里标「已完成」即可；完成时间用审计/修改历史里的记录，不单开一列。和 mainline §3.3「不使用双列、只一个当前有效日期」一致。
- [ ] B. 需要一个【完成日期】列：实际完成那天填，用于精确算延期天数。
- [ ] 其他想法：

> （补充说明，可选）：这一题和 mainline §4「轻量复盘」耦合——状态选四态(B)会顺带定义"延期"在仪表盘上的表达；选三态(A)则延期靠 initial_date 在复盘页算，日常界面不标红（符合 STATE §4）。


### Q18 · 仪表盘查看范围：「对所有人开放」的「所有人」是谁？（⚠️ 必须拍板，牵范围）

**背景**：你在 Q16 写「项目仪表盘的查看功能对所有人开放」。但「所有人」有三种解释，且选哪种会改变要不要新建「访客机制」——这是范围问题，不能自决。已核实现状：Flowboard 未登录直接 401（无访客机制）；`workspace_memberships.role` 只有 admin/member/viewer 三档。

> 假设（mc-expert 默认推荐，低置信）：查看 = 所有 workspace 成员含 viewer，不含未登录访客。viewer 本来就是「只读」语义，仪表盘是只读视图，天然匹配，且不碰认证边界、不开范围漂移口子。

**【你的回答 — 选一个】**

- [ ] **A. 所有 workspace 成员，含 viewer（推荐）**：admin/member/viewer 都能看仪表盘；未登录访客不能看。改动最小，不引入访客机制。
- [√] B. 所有 workspace 成员，**不含 viewer**：只有 admin/member 能看仪表盘；viewer 连看都不行（viewer 只能看别的，仪表盘更敏感）。
- [ ] C. 含未登录访客：任何人打开内网地址都能看仪表盘。→ ⚠️ 这要新建访客/公开分享 token 机制，属于**扩大认证边界**，接近 mainline §5「不做 SSO/原生 App 等已排除产品线」的精神，会显著扩大 feature01 范围。
- [ ] 其他想法：

> （补充说明，可选）：选 C 会牵出独立的访客子系统，不建议塞进 feature01；若真要，应单独立项。mainline §2 现有「非注册用户只读」这句本就和「未登录 401」现状悬空——本题答完，那句也要相应修正。 
> 用户批注：当前只有2种角色：admin/member；暂时无需增加 viewer 角色。所有人 = admin（1名）+（多名注册成员）member


### Q19 · 权限角色对齐：管理员/普通用户 = workspace admin/member？（确认 + admin 名单）

**背景**：Q16 选 A 后，mc-expert 陪审判定（高置信）：**复用 Flowboard 现有 workspace 角色**，不另起一套。「管理员」= `workspace_memberships.role='admin'`，「普通用户」=`member`。改动只是给项目对象加 `created_by` 列 + service 层一个 creator 校验 helper，不动 users 表、不动现有看板/任务权限链。

但有两个前置事实要你确认：

> 假设：feature01 的「管理员/普通用户」就等于 Flowboard 的 workspace admin/member（单工作区，15 人，admin 即项目时间管理者）。

**【你的回答 — 第1点：角色对齐，选一个】**

- [√] **A. 是，管理员=workspace admin，普通用户=workspace member（推荐）**：复用现有角色轴，不引入新角色。
- [ ] B. 不对齐：feature01 要独立的「项目时间管理员」概念，与 workspace admin 解耦（比如某个 member 也该当时间管理员，或某个 admin 不该碰时间轴）。→ 要新建角色体系，改动大。
- [ ] 其他想法：

**【你的回答 — 第2点：workspace admin 名单】**

现状（已核实 `database.py:255`）：seed 默认只有**第 1 个用户 = workspace admin**，其余都是 member。你 Q16 说「管理员可以任意修改所有项目」——如果心智里「管理员」是多人，要明确把谁提为 admin。

- [√] **A. 目前 1 个 workspace admin 就够**（就维持现在的单 admin；feature01 落地后也只有这 1 人能全改所有项目）。
- [ ] B. 需要多个 admin：请在本行后面写出名单或人数（例如「3 个人：张三、李四、王五」），落地时由 admin 在 Flowboard 里提升这些成员的 role。
- [ ] 其他想法：

> （补充说明，可选）：admin 名单属于 Flowboard 现有 workspace 管理功能（admin 可改成员 role），feature01 不需要为此新建 UI——确认后用既有机制提权即可。


### Q20 · created_by 的不可变性（权限模型技术契约）

**背景**：Q16/Q19 拍板后，`created_by`（项目是谁建的）成为写权限的判定依据——member 只能改 created_by=自己的项目。这带来一个必须封死的后门：如果 created_by 本身可以被修改（哪怕只允许 admin 改），那么"想改谁的项目就先把 created_by 改成自己"就能绕过整个权限模型。类比：created_by 像合同上的签名，签了就不能换，签名才有公信力。

> 假设：created_by 在项目首次创建时由系统自动写入，之后**任何人（包括 admin）都不能改**；界面上不提供修改入口，服务端也拒绝任何修改请求。

**【你的回答 — 选一个】**

- [√] **A. 完全不可变（推荐）**：首次创建系统写入后永不可改，admin 也不行。若真发生"建错了人"（比如 A 用 B 的账号建的），只能走"删了重建"。
- [ ] B. admin 可改但强制留审计：留一个后门给极端情况，每次修改记审计。方便，但权限模型有缝，且"改 created_by"这个操作本身就很反直觉。
- [ ] 其他想法：

> （补充说明，可选）：建错人的实际频率很低（单人建项目，登录态即身份），选 A 的"删了重建"代价可接受。


### Q21 · initial_date 纠正权归谁（与新权限模型对齐）

**背景**：`initial_date`（节点首次录入的日期）是延期复盘的基准。mainline §3.3 现有表述是「普通用户不能修改 initial_date；管理员可在首次录错时通过例外入口纠正，并留下审计」。但权限模型升级后（Q16/Q19），这句话里的"管理员"要跟新模型对齐——member 对**自己创建的项目**里录错的 initial_date，能不能自己纠正？还是必须找唯一的 workspace admin？

> 假设：initial_date 纠正权跟项目写权限走同一条线——admin 可纠正任何项目；member 可纠正自己创建的项目（成员少、项目归属清晰时，让创建者自纠最省事，不必事事找 admin）。

**【你的回答 — 选一个】**

- [ ] A. 跟写权限同线：admin 纠正任何项目；member 纠正自己创建的项目。纠正动作本身留审计。
- [√] **B. 只有 workspace admin 能纠正（推荐）**：member 发现录错只能找 admin。权限收得最紧，但当前只有 1 名 admin，纠错都要过他一人。
- [ ] 其他想法：

> （补充说明，可选）：选 A 更顺手且逻辑统一（"你能改这个项目的日期，就能纠它的 initial_date"）；选 B 更保守。两者都保留审计。


### Q22 · Excel 批量导入的项目 created_by 归属

**背景**：Excel 导入是批量建项目的入口（mainline §4.1 必做）。导入时建的项目，`created_by` 算谁？如果简单按"导入操作者=创建者"——设想 admin 替某个 member 导入了他的项目，created_by=admin，那个 member 反而改不了自己的项目。反之如果导入文件里带"创建者"列，任何人导入时都能指定别人当创建者，也是一个口子。

> 假设：第一版导入场景简单（通常各人导自己的，或 admin 统一导入少量项目），从简处理，先不做复杂的归属映射。

**【你的回答 — 选一个】**

- [√] **A. 导入操作者 = created_by（推荐，最简）**：谁点的导入算谁建的。admin 替人导入的项目归 admin 管（member 改不了，需要改就找 admin，符合 admin 全改权）。第一版不提供"指定创建者"功能。
- [ ] B. Excel 里带「创建者」列：导入时可指定每个项目归谁。灵活，但要校验"只有 admin 才能指定别人"，复杂度+1。
- [ ] C. 导入的项目一律 created_by=NULL=任何人可改：省事，但 NULL 语义=无主，权限模型漏一类对象。
- [ ] 其他想法：

> （补充说明，可选）：选 A 后，若将来出现"admin 批量导入全员项目"的真实痛点，再升级 B（受控变更，不破坏兼容）。


## 6. 暂不在本轮决定的技术事项

以下内容留到 UX 与业务规则确认后设计：

- 草稿保存在纯前端内存、浏览器临时存储还是服务端临时区。
- 批量更新 API、并发版本号和事务边界。
- 拖动性能、触屏行为和浏览器兼容实现。
- 审计表结构与活动记录展示格式。

无论最终实现方式如何，都不得破坏“未点击【更新日期】便不改写原始数据”的产品契约。
