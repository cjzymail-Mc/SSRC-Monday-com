# feature01 · UX 收尾细节问卷（轨道控件 / 同日排序 / 色值 / 仪表盘筛选 / Excel 契约）

> 建立日期：2026-08-14  
> 状态：**Q1–Q8 已全部收口（2026-08-18，同轮连带样张遗留 R1–R3）；已落盘（/mc-update 2026-08-18），feature01 进 mainline §6 阶段门 3（技术方案、数据与 API 契约）。** 业务模型、拖动专题、权限模型已在前序问卷全部收口（`2-DRAG_TIMELINE_REQUIREMENTS.md` Q1–Q22）。  
> 讨论对象：类 Excel 时间表编辑器 + 单项目 / 全项目仪表盘的剩余交互细节。  
> 视觉基线样图：`mockup.html`（含双轨编辑器、拖动草稿、重叠方案演示）；Q7/Q8 配对照样图 `dual-track-lane-options.html`。  
> 范围约定：编辑器列宽 / 行高 / 字号等像素级视觉，按 mainline §7 分层原则留到集成验收阶段集中收口，**不在本问卷**；本问卷只定「影响数据行为或页面结构」的项。

---

## 0. 本轮确认记录（2026-08-18 · Q1–Q8 全部收口）

用户对 11 项（Q1–Q8 + 交互样张遗留 R1–R3）一次性拍板「全按推荐冻结」（AskUserQuestion 收口记录，非 Claude 代签）：

1. **Q1 = A**：双轨独立间隔的编辑器交互符合实际操作，按 mock v3.1 交互 + 方向 B 视觉基线推进。视觉验收对象 = `（视觉UI升级留档）single-project-visual-options.html` 方向 B（已选定）+ `（交互样张）node-drag-magnetism.html`。
2. **Q2 = A**：【轨道】单元格下拉选择（`主线 / 并行` 两项，键盘首字母 / 上下键快选）。
3. **Q3 = A**：同日节点按**自然升序**（名称中数字按数值比较，`B2 < B10`；中文按本地文本规则）。
4. **Q4 = A**：六阶段色方向认可（紫/蓝/青/橙/绿/金，v3.2 色板随 2026-08-17 方向 B 拍板转正为当前执行色板）；精确 hex 留集成阶段在真实数据上收口。
5. **Q5 = A**：全项目仪表盘 = 项目多选筛选 + 排序下拉（按开始日期 / 当前阶段先后 / 最近临近节点 / 逾期未完成节点数）+ 克制行内色点（橙 = 7 天内有节点，红 = 有逾期未完成），不染整条泳道，不引入手动优先级字段。
6. **Q6 = A**：Excel 单一格式——一个 sheet 一张大表，首列恒为【项目名称】；【时间间隔】导出计算值、导入忽略；【状态】只接受三态文本，否则按行报错给行号。
7. **Q7 = A（⚠️ 改写已确认项，用户已知悉）**：单/多项目仪表盘统一「默认主线/并行两行 + 展开按钮拆重叠阶段」，取代 PHASE2 §14.6「单项目无限自动分轨」。mainline §4.2/§4.3 相应表述改写 + PHASE2 §14.6「已被取代」注记随 /mc-update 落盘。
8. **Q8 = A**：未展开时**仅重叠段**上下分半（非重叠段满高），颜色纯净、不遮挡，维持 mainline §4.3「不得掩盖重叠」红线。原注记保留：Q7 拍板后，单项目仪表盘「链式 vs 跨式」画法在进技术方案前由 Claude 给对照再定一次（不影响本题重叠渲染原则）。

同轮拍板的交互样张遗留 3 项（`（交互样张）node-drag-magnetism.html`）：

9. **R1**：右键【已完成 / 未完成】切换与日期**共用同一草稿批次**——一次【更新日期】提交、【放弃更改】回滚、提交后一次撤销；审计区分修改类型。样张中的即时切换仅为演示简化，规格以本条为准。
10. **R2**：右键四项菜单（拖拽 / 拖拽顺延 / 已完成 / 未完成）**同样用于全项目仪表盘**，两仪表盘同一套操作。
11. **R3**：单节点档【拖拽】钳制在**前后邻居之间**（前驱与后邻都不可越），链序不变；级联档【拖拽（顺延）】仅受前驱钳制（原 DRAG 确认 2）。

随轮记录（不单独拍板，像素参数按 mainline §7 留集成验收）：磁吸强度默认档 = **标准**（纯 1 日吸附）；放大带 = ±10 天 ×8 倍；已完成节点 = 白色对勾编码。

**至此 Q1–Q8 全部收口，需求侧无已知未决项；待 /mc-update 落盘后过 mainline §6 阶段门 2，进阶段门 3（技术方案、数据与 API 契约）。**

### 拍板速览表（阅读辅助 · 2026-08-14 · 勾选仍以各题完整选项为准）

> ⚠️ 锚定提醒：Q7 要**改写已确认的** PHASE2 §14.6「单项目无限自动分轨」；Q8 的 C 触及 mainline §4.3「不得掩盖重叠」红线。这两题请读完整题干再勾，别被"看起来推荐"带过去。

| 题 | 推荐选项（大白话） | 选其他项的赌注 | 在哪体验 |
|---|---|---|---|
| Q1 | A：双轨交互照旧可用 | B/C 才有返工成本；选 A 零成本 | `mockup.html` v3.1 全部页。**版本注记**：v3.1 仅把【轨道】列换成下拉（Q2 演示）、仪表盘加了排序/色点/两行（Q5/Q7 演示），双轨编辑与顺延交互未变，Q1 验收对象仍成立 |
| Q2 | A：轨道列点开下拉二选一 | B=多一个插行入口，实现多一点；C=「忘了改轨道」静默出错 | `mockup.html` ① 编辑器【轨道】列 |
| Q3 | A：自然排序（B2 < B10） | B=文本序会出现 B10 排 B2 前；C=顺序随录入历史，用户不可预测 | 无需原型：mock 排序即 A 的行为（数字按数值比较已实现）；文字示例 `B2 / B10` 即可判断 |
| Q4 | A：六色方向认可，精确 hex 留集成期 | B=现在锁死，真实数据上大概率要返工；C=下一版并排两组再选（多一轮） | `mockup.html` 任意页图例六色 |
| Q5 | A：筛选 + 排序下拉 + 克制色点 | B=全靠人眼读图，项目多了就累；C=⚠️ 新增手动优先级字段，牵 mainline §5 边界 | `mockup.html` ③ 排序下拉可点 + 行内橙/红小色点 |
| Q6 | A：单一 sheet 大表（首列=项目名称） | B=双格式，解析与行级报错复杂一倍；C=长期维护两套导入识别 | `mockup.html` ① 底部「Excel 格式预览」折叠区 |
| Q7 | A：默认主线/并行两行 + 展开按钮 | B=单项目自动分轨与轨道列语义并存，两套规则；C=自动展开，页面高度随重叠数波动 | 两行模型：`mockup.html` ③（部分演示）；展开↔多行完整体验：`dual-track-lane-options.html` 第一/三节 |
| Q8 | A：仅重叠段上下分半 | B=整条压半高，长项目读感碎；C=⚠️ 徽标遮盖，默认视图看不到被盖阶段=放宽 mainline §4.3 | `dual-track-lane-options.html` 第二节三方案放大对照 |

> Q8 连带影响注记：`mockup.html` 现行「节点链连续条形」中相邻条首尾相接、本身不重叠；Q8 三方案针对的是「阶段跨条」画法（对照稿即此画法）。Q7 拍板后，单项目仪表盘最终用链式还是跨式画法，在进入技术方案前由 Claude 给对照再定一次（不影响本题先拍重叠渲染原则）。

---

## Q1 · 「双轨独立间隔」的编辑器 mock 是否符合实际操作（整体验收题）

**背景**：mock v3 已实现主线 / 并行双轨、两轨各自计算间隔、各自顺延，及日期 / 阶段双排序按钮。STATE.md §7 把「确认 mock 符合实际操作」列为第一未决项——这是对已冻结业务模型（mainline §3.2）的交互层验收。

> 假设：双轨业务模型已冻结，本题只验收「呈现与操作手感」，不重开模型讨论。

**【你的回答 — 选一个】**

- [√] **A. 符合实际操作，按当前 mock 交互继续推进（推荐）**：双轨呈现、间隔计算、顺延反馈与我的实际用法一致。
- [ ] **B. 大体符合，但有一处要调**（请在补充说明里指明哪一处：轨道列位置 / 间隔显示方式 / 顺延反馈 / 排序按钮……）。
- [ ] **C. 不符合，需要重新讨论交互**（说明卡在哪）。
- [ ] 其他想法：

> （补充说明，可选）：

---

## Q2 · 【轨道】字段的编辑控件

**背景**：轨道是每行必填字段（第一版仅 `主线 / 并行` 两个值）。新增行或修改行时用什么控件选轨道，直接影响「像 Excel 一样快」的核心诉求。已确认的「在某行下方插入节点 / 新增到末尾」两种插入方式（PHASE2 §12.2）不含轨道的自动带出。

> 假设：第一版只有两个轨道值，不出现第三轨；控件须同时适合鼠标和键盘操作。

**【你的回答 — 选一个】**

- [√] **A. 单元格下拉选择（推荐）**：点【轨道】单元格弹出 `主线 / 并行` 两项；键盘可用首字母 / 上下键快选。实现最简单、防错，第一版够用。
- [ ] **B. 下拉 + 「插入并行节点」快捷新增**：在 A 基础上，每行操作菜单加一个【在此后插入并行节点】，自动带出 轨道=并行、日期=上一并行节点日期，少一次手填。
- [ ] **C. 智能默认 + 下拉修正**：新增行自动继承上一行轨道，仅在需要时下拉改；最快但可能造成「忘了改轨道」的静默错误。
- [ ] 其他想法：

> （补充说明，可选）：

---

## Q3 · 同日节点的稳定排序规则（定死规则）

**背景**：同一轨道内同日多节点时必须有确定的先后（间隔=0 天）；它同时决定编辑器显示顺序和 Excel 导出行序。PHASE2 早期批注写「同日按节点名称升序（B1/B2/B3）」，§13.5 又推荐「自然排序」——对 `B2 / B10` 这类名称两者结果不同，需要定死一种。Q7 已拍板同日不用拖动手柄、靠排序规则稳定。

> 假设：规则只需覆盖「同一轨道 + 同一日期」的节点；跨阶段同日不比较顺序（阶段排序视图按 mainline §4.1 只改显示）。

**【你的回答 — 选一个】**

- [√] **A. 自然升序（推荐）**：名称中的数字按数值比较（`B2 < B10`），中文按本地文本规则；与早期「名称升序」批注对纯中文名一致，且不会出现 B10 排在 B2 前的怪序。
- [ ] **B. 普通文本升序**：严格按字符串比较（`B10 < B2`），与 Excel 默认排序行为完全一致。
- [ ] **C. 同日按创建先后**：最早创建的排前；顺序取决于录入历史，用户不可预测。
- [ ] 其他想法：

> （补充说明，可选）：

---

## Q4 · 六阶段色值的确认方式

**背景**：六阶段专属色是仪表盘的核心视觉语言（mainline §4.2/§4.3），mock 当前候选为 紫（创意）/ 蓝（设计）/ 青（开发）/ 橙（测试）/ 绿（量产）/ 金（应用迭代）。mainline §7 已分层：原型阶段定方向、集成阶段锁像素。

> 假设：六阶段颜色语义（哪个阶段对应哪类色相）现在定方向即可；精确 hex 在真实数据验收时统一收口。

**【你的回答 — 选一个】**

- [√] **A. 认可当前方向（推荐）**：紫 / 蓝 / 青 / 橙 / 绿 / 金 的阶段对应关系成立，最终色值留给集成阶段在真实数据上微调。
- [ ] **B. 现在就锁色值**：我在下一版 mock 给出 6 个精确 hex 的对照表（含浅 / 深背景两套），你逐个拍板。
- [ ] **C. 换方向**：当前对应关系有不合适的（哪个阶段的颜色感觉不对，写在补充说明）；下一版 mock 并排 2 组候选再选。
- [ ] 其他想法：

> （补充说明，可选）：

---

## Q5 · 全项目仪表盘的筛选、排序与「轻重缓急」表达

**背景**：mainline §4.3 要求全项目仪表盘能「快速识别临近节点、延期项目、当前阶段和轻重缓急」，但筛选控件、排序依据、轻重缓急怎么表达未定。单项目仪表盘无筛选需求，不在本题。

> 假设：不引入用户手填的「优先级 / 紧急程度」字段——那会滑向项目管理套件（mainline §5 边界）；轻重缓急只能由日期数据推导。

**【你的回答 — 选一个】**

- [√] **A. 筛选 + 排序下拉 + 克制行内提示（推荐）**：顶部项目多选筛选（沿用已确认的多选）；排序下拉提供 按开始日期 / 按当前阶段先后 / 按最近临近节点 / 按逾期未完成节点数；行内只用小色点提示（橙点=7 天内有节点，红点=有逾期未完成），不染整条泳道——与「红色克制」原则一致。
- [ ] **B. 只做筛选 + 固定排序**：按项目开始日期固定排序，轻重缓急完全靠人眼读图（绝对日历标尺 + 今日线已能支撑）。
- [ ] **C. 增加项目级手动「置顶 / 关注」标记**（⚠️ 牵范围：新增数据字段且接近手动优先级，与 mainline §5 「完整项目管理套件」边界需用户显式拍板）。
- [ ] 其他想法：

> （补充说明，可选）：

---

## Q6 · Excel 导入导出的最小格式契约

**背景**：Excel 只作导入 / 导出格式（mainline §4.1）。你现在的 Excel 两种组织都有（每项目一个文件 / 一份含多项目，PHASE2 §3.3）。导入契约必须定：文件怎么组织、列怎么映射、哪些列导入时被忽略。

> 假设：  
> 1. 列 = 源数据表 8 列：`项目名称｜项目阶段｜轨道｜项目节点｜日期｜时间间隔｜状态｜备注`；  
> 2. 【时间间隔】导出为计算值、**导入时忽略**（间隔永远由日期链推出，手填值不作输入，避免两套真相）；  
> 3. 【状态】导入只接受三态文本（未开始 / 进行中 / 已完成），其余值按行报错并给行号；  
> 4. 权限按已冻结契约：created_by = 导入操作者（DRAG Q22）。

**【你的回答 — 选一个】**

- [√] **A. 单一格式：一个 sheet 一张大表（推荐）**：无论单项目还是全项目，第一列永远是【项目名称】；单项目导出 = 同格式只含该项目的行。导入导出只有一种解析器，最不容易错。
- [ ] **B. 双格式**：单项目 = 一个文件（无【项目名称】列或恒定）；全项目 = 每项目一个 sheet。更贴近你现有两种 Excel 习惯，但解析与错误提示复杂一倍。
- [ ] **C. 导入兼容 A/B 两种，导出只用 A**：兼顾存量文件与长期单一格式，导入器要做两种识别。
- [ ] 其他想法：

> （补充说明，可选）：

---

## Q7 · 仪表盘轨道行模型：默认「主线/并行」两行 + 展开按钮（⚠️ 调整已确认项，必须拍板）

**背景**：用户指出【轨道】列拍板后，泳道应直接等于轨道——主线恒在第 1 行、并行在第 2 行（业务上双轨已覆盖实际项目复杂度）。而 `1-PHASE2_REQUIREMENTS_DRAFT.md` §14.6 曾拍板「单项目仪表盘按重叠**自动分轨、不限行数**」——那是【轨道】列出现之前、只能靠日期重叠反推泳道的方案。现提案合并两层：泳道=轨道（默认每项目固定 2 行），阶段重叠降级为"行内渲染 + 展开按钮"。多项目「最多两轨 + 局部展开」与既有拍板一致，不受影响。对照效果见 `dual-track-lane-options.html` 第一节 / 第三节。

> 假设：轨道列（主线/并行）是泳道的唯一分配依据；阶段重叠只影响行内渲染与展开，不再自动生成新泳道；默认收起、每项目独立展开、展开不影响其他项目行高。

**【你的回答 — 选一个】**

- [√] **A. 采纳「默认两行（主线/并行）+ 展开按钮拆重叠阶段」（推荐）**：单/多项目仪表盘统一模型；默认最紧凑，展开后每阶段独占一行完全可读。将改写 §14.6 的「单项目无限自动分轨」表述。
- [ ] **B. 维持 §14.6 原拍板**：单项目仪表盘默认就按重叠自动分轨、不限行数；多项目保持两轨 + `+N`。两套规则并存。
- [ ] **C. 折中**：默认两行，但进入页面时**自动展开**存在重叠的项目；用户可手动收起。
- [ ] 其他想法：

> （补充说明，可选）：

---

## Q8 · 未展开时，主线行内的阶段重叠怎么渲染（⚠️ 与 mainline §4.3 相关）

**背景**：默认态主线一行内可能出现阶段重叠（如创意尚未结束、设计已开始）。mainline §4.3 红线要求「仪表盘必须真实表达并行状态，不得通过修改日期或混合阶段颜色掩盖重叠」，这排除了"后画盖前画且不提示"的做法。三个方案的放大对照见 `dual-track-lane-options.html` 第二节。

> 假设：展开后每阶段独占一行、重叠完全可读；本题只定「未展开的默认行」；行高默认不变、六阶段永远用原色不混色。

**【你的回答 — 选一个】**

- [√] **A. 仅重叠段上下分半（推荐）**：非重叠段满高、重叠段两阶段各占半高；颜色纯净、无遮挡。Google Calendar / ProductPlan 同类做法。
- [ ] **B. 整段分半**：凡参与重叠的阶段整条降为半高；实现最简单，但未重叠部分也被压细。
- [ ] **C. 徽标提示 + 展开**：默认整条满高、后画覆盖先画，重叠处只标 ⧉N（⚠️ 默认视图看不到被盖阶段，与 mainline §4.3「不得掩盖重叠」存在张力，选此须显式接受并视为对该红线的放宽）。
- [ ] 其他想法：

> （补充说明，可选）：

---

## 附：Claude 的备注（用户不用填）

- 填完后 Claude 按问卷模式评估：找矛盾（与冻结主线冲突、题间冲突）、找空白、找连锁影响；确认项回填 §0 并同步 feature01 `STATE.md` / mainline（若涉及）。
- Q1–Q8 全部收口后，feature01 需求侧无已知未决项，可进 mainline §6 阶段门 3（技术方案、数据与 API 契约）。
- Q7 若选 A/C，需同步改写 mainline §4.2/§4.3 表述并给 `1-PHASE2_REQUIREMENTS_DRAFT.md` §14.6 加「已被取代」注记（拍板后由 Claude 落盘）。
- 本问卷优先级低于 `mainline-feature01.md` 与 feature01 `STATE.md`。
