# feature01 冷启动状态

> 最后更新：2026-08-14（DRAG Q1–Q22 收口 + 权限基线落盘；剩余 UX 未决项落成 `3-UX_DETAIL_REQUIREMENTS.md` 问卷 Q1–Q8 待用户填答，§0 附拍板速览表；mock 升 v3.1 克制加入 Q2/Q5/Q6/Q7 推荐演示——陪审判定"mock 演示推荐项→问卷拍板→回填"不算代签，演示均标注待拍板、备选项保留在对照稿 `dual-track-lane-options.html`；三份需求文档已按用户重命名加数字前缀 1-/2-/3-）
>
> 当前阶段：**产品主线已建立，需求与 UX 原型继续收敛；未进入技术方案或正式开发。**
>
> 新会话先读本文件即可接手；需要追溯讨论再读 `PHASE2_REQUIREMENTS_DRAFT.md`。

## 1. 产品目标与边界

在已交付的 Flowboard 上叠加鞋类研发项目的**时间节点编辑 + 单项目/多项目仪表盘**，服务约 10～15 人的局域网团队、约 10 个并行项目。

- 网页内提供类 Excel 编辑器；Excel 只作导入导出。
- 不接 Google 文档，不另做独立产品。
- 不扩成完整项目管理套件；不做 OKR、工时、预算、资源负载、审批等。
- 当前只做需求和静态原型；用户确认前不改主程序和数据库。

## 2. 已确认的业务模型

### 六阶段与节点

- 固定阶段：`创意 → 设计 → 开发 → 测试 → 量产 → 应用迭代`，不可增减改序。
- 一行一个单日里程碑节点；每个节点必须有阶段和节点名称。
- 阶段业务顺序固定，但日期允许重叠，仪表盘必须真实表达并行。

### 源数据表

当前推荐并已在 mock 中采用：

```text
项目名称｜项目阶段｜轨道｜项目节点｜日期｜时间间隔｜状态｜备注
```

- 用户日常只编辑一个【日期】，不再使用计划/实际双列。
- 【轨道】第一版只有 `主线 / 并行`。
- 两条轨道共用同一套节点、日期、间隔字段，不设置两套重复列。
- `initial_date` 仅在系统内部保存：新节点首次保存时自动建立；普通用户无感，管理员只能以审计方式纠正首次录错。
- 不强制填写修改原因；需要说明时使用现有【备注】列。

### 节点状态（2026-08-14 确认）

- 三态枚举：`未开始 / 进行中 / 已完成`。日期 ≥ 今天 = 未开始；日期 < 今天且未手工标完成 = 进行中（隐含逾期）；手工标完成 = 已完成。
- 不单开【完成日期】列；完成时间由审计/修改历史承载，与单日期模型一致。

### 双轨日期与间隔

```text
主线：A → B → C
并行：P1 → P2 → P3
```

- 【间隔】只有一列，表示“当前节点与**同轨道上一节点**的日期差”。
- 每条轨道首节点的间隔为空。
- 改 B 日期：重算 B 间隔，只顺延 C 及后续主线节点。
- 改 B 间隔：重算 B 日期，只顺延 C 及后续主线节点。
- 并行轨完全沿用相同规则；改 P2 只影响 P3 及后续并行节点。
- 主线与并行互不计算间隔、互不顺延。
- 只改日期则日期优先；只改间隔则间隔优先；两者同时改则日期优先并覆盖重算间隔。
- 后续节点整体平移，后续各段原间隔保持不变；保存后提供一次撤销。
- 删除节点不移动其他日期，只重算该轨道合并后的间隔。

### 排序

编辑器用独立按钮切换：

1. 按日期排序（默认）。
2. 按六阶段分组，阶段内按日期升序。

排序只改变显示，不修改日期、间隔或审计数据。每个节点的间隔始终根据本轨道真实时间前驱计算。

## 3. 仪表盘与视觉

- 两种仪表盘：单项目、全部/多选项目。
- 使用共享的绝对日历标尺和红色今日线。
- 六阶段使用稳定专属色：当前候选为紫、蓝、青、橙、绿、金。
- 时间轴使用连续条形；条形已拍板为 **2px 小圆角矩形**。
- 单项目完整展示主线和并行轨。
- 多项目默认最多显示两轨；当前业务假设现实中最多主线 + 并行双轨。
- 不用透明混色承担主要信息表达。

## 4. 轻量延期复盘方向

不建设企业级 baseline、定期快照或复杂 BI：

- 日常界面只展示当前有效日期，不满屏标红延期。
- 内部保留 `initial_date`、当前 `date` 和结构化修改活动。
- 级联顺延记录为一次操作批次，区分直接修改与自动影响。
- 第一版复盘只需按单/多项目筛选，查看初始日期、当前日期、净变化天数、直接修改次数和修改历史。

此处的“延期”严格说是“相对首次录入日期的变化”，这是当前轻量方案可接受的限制。

## 5. 权限与工程红线

- **权限模型（2026-08-14 用户拍板升级，mainline §2/§5/§9 已落盘）**：复用 Flowboard workspace 角色（admin/member，当前团队仅这两种、1 名 admin）。查看：仪表盘与编辑器对所有注册成员开放，未登录不可见，不引入访客机制。写：admin 可改/拖动所有项目；member 仅可改/拖动自己创建的项目（created_by）。
- **权限技术契约（已冻结，DRAG Q20–Q22）**：created_by 首次创建系统写入后永不可改（含 admin）；initial_date 例外纠正权仅限 workspace admin；Excel 导入项目 created_by = 导入操作者，第一版不做指定创建者。
- 已核实：Flowboard 核心对象（boards/tasks/groups）现无 created_by 列，需 schema 加列 + service 层 creator 校验（技术方案阶段细化实现细节）。
- 权限必须服务端执行。
- 所有修改可追踪，删除优先软删除。
- schema 迁移前必须备份且可恢复；不得破坏第一阶段旧数据。
- 后续每个实现检查点必须同时包含数据层、API、可操作 UI、测试和文档。

## 6. 当前产物

- `mainline-feature01.md`：feature01 冻结主线与完成边界。
- `1-PHASE2_REQUIREMENTS_DRAFT.md`：完整需求问答与用户批注（追溯用长文档，已加 2026-08-14 同步注记标出被后续拍板取代的段落），仅在追溯时读取。
- `2-DRAG_TIMELINE_REQUIREMENTS.md`：拖动专题 + 权限模型问卷，Q1–Q22 已全部收口（确认记录在其 §0）。
- `3-UX_DETAIL_REQUIREMENTS.md`：剩余 UX 细节问卷（**当前活跃**），Q1–Q8 待用户填写。
- `mockup.html`：静态交互样图 **v3.1**（视觉基线），不接后端；含编辑器、单项目、全项目、重叠方案对比。v3.1 新增 Q2（轨道列下拉）/ Q5（排序下拉 + 克制色点）/ Q6（Excel 格式预览折叠区）/ Q7（全项目每项目主线/并行两行，部分演示）推荐演示，**均标注待拍板，拍板前不是确认需求**。
- `dual-track-lane-options.html`：Q7/Q8 对照样图（默认两行 + 展开按钮完整体验；行内重叠渲染三方案），拍板后降级留档（同 `drag-timeline-options.html` 待遇）。

**过程注记（2026-08-14 · mc-expert 陪审结论，轻量固化）**：「mock 演示推荐项 → 问卷拍板 → 回填」不算事实代签，附三条件——① mock 内演示显式标注「待拍板」；② 备选项必须仍可看（对照稿保留 + 速览表给赌注）；③ 改动聚块、备选被否时能便宜回滚。稳定验证 2–3 次后再议升入 AGENTS.md §2（治理文件不代改，届时先与用户对齐）。

mock 当前已演示：

- 主线/并行轨道列。
- 两轨分别计算日期和间隔、分别顺延。
- 日期/阶段排序按钮。
- 2px 小圆角。
- 重叠方案对比和局部展开样例。

## 7. 下一会话继续讨论

剩余 UX 未决项已全部落成 `3-UX_DETAIL_REQUIREMENTS.md` 问卷（Q1–Q8），**当前等用户填答**，不进入编码：

1. Q1：双轨独立间隔的编辑器 mock 是否符合实际操作（整体验收题）。
2. Q2：【轨道】字段的编辑控件（下拉 / 快捷新增并行节点 / 智能默认）。
3. Q3：同日节点的稳定排序规则（自然升序 / 文本升序 / 创建先后）。
4. Q4：六阶段色值的确认方式（方向认可 / 现在锁 hex / 换方向）。
5. Q5：全项目仪表盘的筛选、排序与轻重缓急表达（⚠️ C 选项牵 mainline §5 范围边界）。
6. Q6：Excel 导入导出最小格式契约（单一 sheet 大表 / 双格式 / 导入兼容两种）。
7. Q7：仪表盘轨道行模型「默认主线/并行两行 + 展开按钮」（用户 2026-08-14 主动提出；⚠️ 调整 PHASE2 §14.6「单项目无限自动分轨」旧拍板，须显式拍板；配样图 `dual-track-lane-options.html`）。
8. Q8：未展开时主线行内阶段重叠的渲染方式（仅重叠段分半 / 整段分半 / 徽标 ⚠️ 放宽 mainline §4.3）。

编辑器列宽、行高、字号等像素级视觉按 mainline §7 分层原则留到集成验收阶段收口，不在本问卷。

Q1–Q6 收口后，需求侧无已知未决项，进 mainline §6 阶段门 3（技术方案、数据与 API 契约）；技术数据模型、API、并发冲突和迁移方案届时再设计。

## 8. 文件优先级

冲突时：

```text
用户最新明确指令
→ mainline-feature01.md
→ PHASE2_REQUIREMENTS_DRAFT.md 中已确认批注
→ 本 STATE.md
→ mockup.html 探索样图
```

样图用于提问和验证，不能自行覆盖已确认主线。
