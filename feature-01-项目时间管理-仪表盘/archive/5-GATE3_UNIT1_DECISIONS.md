# feature01 · 阶段门 3 单元 ①「领域对象与派生规则」技术拍板问卷

> 建立日期：2026-08-18（同日改写为通俗版：每题补充「这题在问什么」的白话解释与推荐理由）  
> 状态：**D1–D14 已全部收口（2026-08-18 用户一次性拍板，确认记录见 §0）；单元 ① 仅剩「链式 vs 跨式」画法（D13 只锁了数据契约，画法等对照稿 `（画法对照）stage-bar-chain-vs-span.html` 再定）。**  
> 讨论对象：`4-GATE3_UNIT1_TECH_DRAFT.md`（planner 草案 v1 + 主 Claude 抽查复核）§0 决策点速览表 D1–D14——题目编号与草案一一对应，每题自带通俗解释、例子和推荐理由，不读草案也能拍；想深究再按「详见草案 §x」跳转。  
> 模式说明：按用户 2026-08-18 指令，门 3 技术拍板**沿用问卷往返形式**（AGENTS.md §2 格式；其「仅限调研阶段」的限定由用户指令在本 feature 内放宽，治理文件本身不代改）。  
> 范围约定：本问卷只拍单元 ①。单元 ②（批量 API / 并发 / 撤销护栏）与单元 ③（迁移 runbook / Excel 细则 / 回归清单 / 深色范围）的拍板项后续另出；链式 vs 跨式的**最终画法**仍按 mainline §8 由 Claude 出 HTML 对照稿后再定一次（D13 只锁 API 数据契约，不预支画法决策）。

---

## 0. 确认记录（2026-08-18 · D1–D14 全部收口）

用户对 14 项一次性拍板（8 项单独拍 + P1 六项打包；D10 为用户修订而非推荐项）：

1. **D1 = A**：数据模型 = **新增专表**（timeline_projects / timeline_nodes + 批次审计两表，v16 纯增量迁移）；间隔/状态不落库、实时派生。
2. **D2 = A**：项目 = **独立实体**，不与 board 挂钩；未来挂钩走受控变更（加列）。
3. **D4 = A**：已完成载体 = **done_at 内部时间戳**；同时**认定 done_at 不违反「不单开完成日期列」**（界面上永不出现完成日期列/输入框，仅复盘页间接可见「何时标的完成」）。
4. **D7 = A**：当前阶段 = **已动工阶段中六阶段序最大者（跨两轨）**；无已开始节点 →「未开始」。知情接受：早阶段并行尾巴在一格提示中被序最大吞掉（泳道展开仍可见）。
5. **D8 = A**：最近临近节点 = **date≥今天且未完成中最早者；同日多节点全部返回**，排序键 (日期, 阶段序, 主线优先, 节点名)；空 → 按项目开始日期兜底。
6. **D10 = 修订（用户改推荐，覆盖同日 Q5 的「7 天内」表述）**：橙点窗口 = **本周（周一 ~ 周日，含今天所在自然周）**，不再用滚动 7 天窗口。Claude 按用户意图推导的配套细则（**可否决**）：① 橙 = 本周内有**未完成**节点，**含本周已过去的天**（周一到昨天没做完的也是本周工作）；② 红点不变（date<今天且未完成，不限本周）；③ 红橙语义独立、可并存（本周内过期的未完成节点两点同亮），显示红左橙右。mainline §4.3 与 `3-UX_DETAIL_REQUIREMENTS.md` §0 记录 5 已加覆盖注记。
7. **D12 = B**：Excel 导入状态矛盾 = **日期优先，只认「已完成」**；非三态文本仍行级报错；导入预览明示「状态将按日期重算」。
8. **D13 = A**：API 恒返 **nodes[] + segments[] 双份**；链式/跨式 = 服务端可切换段算法，画法决策与数据契约解耦。
9. **P1 = A**：D3（service 层三重锁 + 不用触发器）/ D5（批次专表 + direct/cascaded 行角色 + audit_log 桥接）/ D6（开始日期=两轨全部节点 min(date)，零节点→NULL 排末）/ D9（逾期数两轨合并、红点阈值≥1）/ D11（指标+间隔实时计算不落库）/ D14（今天=服务器 Asia/Shanghai 本地日、前端不自算）六项**打包冻结**。

随轮记录的两个**未来受控变更候选**（用户提出、明确"未来预留"，不进 feature01 完成边界，升级须用户显式拍板）：

- **每周看板（本周工作提示）**：以 D10 本周窗口为语义地基；feature01 只做橙点，不建每周看板页面。
- **md/json 导入**：未来提供空白标准模板，先让 agent 清洗数据再上传；feature01 v1 维持 Q6 冻结的 Excel 单一格式。D12=B 的状态语义与文件格式无关，天然兼容。

**至此单元 ① 决策面收口（仅剩画法对照稿）；派生指标契约冻结为服务端唯一实现，表结构三件套转正为单元②/③ 地基。**

### 拍板速览表（阅读辅助 · 2026-08-14 出题版 · 勾选结果以各题 √ 为准）

> ⚠️ 锚定提醒：D4 触及「不单开完成日期列」的表述边界（需你认定 done_at 不算违反）；D7 的推荐取法会吞掉早阶段并行轨的领先信息（提示语义层面取舍）；D12 的选项字母沿用草案（**推荐项是 B 不是 A**），别顺手勾 A。

| 题 | 推荐选项（大白话） | 为什么推荐（一句话） | 选其他的赌注 |
|---|---|---|---|
| D1 | A：给时间轴新建账本（新专表） | 纯"加盖一页"，第一阶段零触碰，现有测试原样跑过=回归验收 | B：看板全部查询加过滤 + 双投影包袱 + 旧板回补创建者 |
| D2 | A：项目=独立新东西，不绑看板 | 看板归看板、项目归项目；以后想挂钩加一列即可 | B：与 D1-B 代价绑定 |
| D4 | A：存"打勾那一刻"的时间戳 | 一个字段同时当开关+取证；用户不可见，不算新增列 | B：复盘多查一跳；C：需重新表述冻结条款 |
| D7 | A：已动工阶段里排最靠后的 | 项目进度由最深入的方向代表，算法稳定不抖动 | B：同日多节点时结果跳；C：并行领先时说谎 |
| D8 | A：今天(含)起最早的未完成，同日全给 | 最急的不漏、并列的不丢 | B：今天到期不提醒，反直觉；C：静默丢信息 |
| D10 | A：今天 ~ 今天+6，恰 7 天 | 字面最贴"7 天内"；只算要催的 | B：实际变 8 天；C：橙点失真 |
| D12 | **B**：日期优先，只认「已完成」 | 唯一算不出的是"已完成"必须收；其余听日期=与编辑器同一套规则 | A：无害报错轰炸；C：重开已冻结决策 |
| D13 | A：节点清单+算好的区段清单双份给 | 画法以后换主意只切算法开关，两个仪表盘同源不分叉 | B：规则散到前端两处实现；C：堵住单元②③ |
| P1 | A：六项高置信打包冻结 | 都是"实现层怎么落"，有代码事实/已拍条款直接支撑 | 写编号单独展开 |

**填答方式**：把选中项的 `[ ]` 改成 `[√]`，自由想法写在「补充说明」或「其他想法」处，保存即可。填完后 Claude 按问卷模式评估（找矛盾/空白/连锁影响）→ 回填本 §0 → 同步草案/STATE/mainline。

---

## D1 · 数据模型落位：给时间轴新建账本，还是记在现有看板的账本上

**这题在问什么**：数据库里现在有一套为「看板卡片」设计的表（boards/tasks）。feature01 的项目时间轴是另一种东西——一行一个**单日里程碑**，而且「间隔」「状态」都是按日期**算出来**的、根本不用存。现在要定：时间轴的数据放哪。

**打个比方**：公司原本有一本「看板账」，每页是卡片。现在要记的是「项目节点流水」（某项目、某阶段、哪天到节点）。可以记在**新账本**上，也可以**挤进旧账本**（每行加个记号区分"这是看板卡还是时间轴节点"）。

> 假设：feature01 的 8 列字段（项目/阶段/轨道/节点/日期/间隔/状态/备注）用不上看板的动态字段、视图、评论那一套；间隔与状态不落库是已冻结条款。

**【你的回答 — 选一个】**

- [√] **A. 新账本：新增专表（推荐）**——timeline_projects / timeline_nodes 加批次审计两表，数据库 v16 迁移纯"加盖一页"。**推荐理由**：① 最安全——第一阶段看板功能一个字不用改，现有 79+5 个测试原样跑过就等于回归验收；挤进旧账则看板/甘特/搜索/导出的**每个**查询都得加"别把时间轴节点算进来"的过滤，13 个测试文件全要动，改漏一处看板上就冒出不该有的节点。② 最省事——现有 tasks 每改一次要同时写两处（任务列+动态字段表），这是看板体系的历史包袱，时间轴用不上何必背。③ 权限干净——"member 只能改自己建的项目"落在新表 created_by 一列上，一眼可查；混进旧表要跟看板的"开放可写"规则打架。
- [ ] **B. 挤进旧账本：复用 boards/tasks 加列**——boards 当项目、tasks 当节点，加轨道/阶段等列 + 「kind=timeline」记号列。**赌注**：上面三条全反过来；另外现有 2 块旧板还得补决定"算谁创建的"。
- [ ] 其他想法：

> （补充说明，可选）：

*（详见草案 §2）*

---

## D2 · 「项目」就是一块看板吗

**这题在问什么**：第一阶段你们有「看板」（board），feature01 有「项目」。直觉上容易觉得"一个项目 = 一块看板"，但这会把 D1 选 B 的全部代价绑进来。这题单独确认「项目」的身份。

> 假设：第一版不要求"同一项目既看板又时间轴"同屏联动；未来真需要时加一列映射就能受控升级，现在不预建。

**【你的回答 — 选一个】**

- [√] **A. 项目 = 独立的新东西，不与看板挂钩（推荐）**——一个项目一行 timeline_projects，与 board 零外键；10 个并行项目 = 10 行；mock 里「项目时间轴」本来就是独立入口。**推荐理由**：你的 10 个研发项目要的是日期链+仪表盘，不是卡片列——硬绑看板得不到任何本功能的好处，却继承看板的全部包袱（D1-B 三条代价）。以后真想"给项目挂块讨论看板"，加一列映射即可，现在不用预支这个复杂度。
- [ ] **B. 一项目 = 一 board**：时间轴与看板同容器。**赌注**：接受 D1-B 的回归面与双投影代价，且现有 board 的"开放可写"和项目的"创建者可写"两套权限在同一容器里打架。
- [ ] 其他想法：

> （补充说明，可选）：

*（详见草案 §2）*

---

## D4 · 「已完成」在库里怎么存（⚠️ 顺带请你认定一个表述边界）

**这题在问什么**：三态里「未开始/进行中」按日期算、不用存；「已完成」是人工打的勾，**必须存**。推荐存**打勾那一刻的时间戳**（字段名 done_at）——一个字段同时回答两个问题：「做完没有」（有没有时间戳）和「什么时候标的完成」（时间戳本身）。

**需要你认定的边界**：你之前拍过「不单开【完成日期】列」。done_at 是**内部记账字段**——界面上永远不会出现让你看或填的"完成日期"，你只会在右键菜单点【已完成/未完成】；只有复盘页想看"何时标的完成"时它才被读出来。**选 A 即认定这不违反该条款**。

> 假设：用户界面永远不出现「完成日期」列或输入框；done_at 只以"何时标的完成"的形式在复盘页间接可见。

**【你的回答 — 选一个】**

- [√] **A. 存时间戳 done_at（推荐）**——打勾=记时刻，取消勾=清掉。**推荐理由**：一列干两件事（开关+取证）；复盘问"这个节点什么时候标的完成"直接有答案，不用去审计流水里再对一次。右键【未完成】就是把它清空，操作零变化。
- [ ] **B. 存 0-1 开关**：只记"完没完成"。**赌注**：查询稍省一点，但"何时标的完成"要靠审计批次表倒推，复盘时多一跳、易错。
- [ ] **C. 可以用 done_at，但我认为它算新增列**——需要重新表述「不单开完成日期列」条款（请在补充说明写你的表述）。
- [ ] 其他想法：

> （补充说明，可选）：

*（详见草案 §3）*

---

## D7 · 「当前阶段」怎么算（阶段重叠、双轨并行时）

**这题在问什么**：全项目仪表盘按"当前阶段先后"排序、单项目提示"现在处于哪个阶段"——都得把一个项目压成**一个**阶段值。但你们明确允许阶段日期重叠、两轨并行，所以必须定算法。

**举个例子**：跑鞋 A 主线还在「开发」，并行轨（比如鞋底模具）已经进了「测试」——仪表盘这一格显示什么？

> 假设：这是"一格提示/一个排序键"，不承担完整呈现并行进度的职责——并行详情靠泳道内两行+展开按钮表达。

**【你的回答 — 选一个】**

- [√] **A. 已动工的阶段里，六阶段排最靠后的那个（推荐）**——某阶段只要有一个节点日期 ≤ 今天就算"已动工"；当前阶段 = 已动工者中排位最深的（两轨一起看）。都没有 → 显示「未开始」。**推荐理由**：项目整体进度由**最深入的方向**代表——并行都进测试了还说项目在开发，等于低估；而且这个算法结果稳定，不会因为同一天有几个节点就跳来跳去。**知情赌注**：反过来也一样——主线进了「量产」、并行轨还剩个「设计」尾巴没收尾时，显示「量产」，那条尾巴在这一格看不见（泳道展开仍能看到）。
- [ ] **B. 最新日期节点的阶段**：直观。**赌注**：同日多节点/两轨交错时今天显示这个明天显示那个，排序抖动。
- [ ] **C. 主线轨优先**：**赌注**：并行领先时直接说谎——并行已到测试还显示开发，违背你拍过的「仪表盘必须真实表达并行」。
- [ ] 其他想法：

> （补充说明，可选）：

*（详见草案 §5）*

---

## D8 · 「最近临近节点」怎么挑

**这题在问什么**：这是全项目排序键之一，也是单项目"下一节点是什么"的答案来源。要定两点：今天到期的算不算"临近"、同一天好几个节点怎么办。

**举个例子**：今天 8-18。某项目未完成节点：8-18 一个、8-20 一个（另有 8-22 一个已标完成）。

> 假设：今天到期且未完成 = 最紧急，必须被提示到。

**【你的回答 — 选一个】**

- [√] **A. 今天(含)起最早的未完成节点；同日多个则全给（推荐）**——上例挑 8-18；若 8-18 当天有 3 个节点，3 个都返回（界面可显示「今天 ×3」），排序按 (日期, 阶段序, 主线优先, 节点名)。全过去了/全完成了 → 空，全项目排序按项目开始日期兜底。**推荐理由**：到期提醒的全部价值在"最急的别漏"；同日全给让你自己决定先盯哪个，信息不丢。
- [ ] **B. 严格"今天之后"才算**：8-18 到期的不提醒。**赌注**：反直觉——最急的一天反而静默。
- [ ] **C. 同日只挑一个**（比如阶段最早的）。**赌注**：并列的其他节点静默消失，你不知道还有俩。
- [ ] 其他想法：

> （补充说明，可选）：

*（详见草案 §5）*

---

## D10 · 「7 天内有节点」的 7 天，从哪天到哪天

**这题在问什么**：全项目行内**橙点**的判定窗口。"7 天内"人人能懂，但落到算法有两派：含今天共 7 个日历日，还是从今天起数 7 整天后（实际 8 个日历日）。

**举个例子**：今天 8-18。A = 8-18 ~ 8-24（恰好 7 格）；B = 8-18 ~ 8-25（8 格）。

> 假设：橙点是"要催的事"的紧急提示——已完成的节点不用催。

**【你的回答 — 选一个】**

- [ ] **A. [今天, 今天+6]，恰好 7 个日历日、只算未完成（推荐）**。**推荐理由**：字面最贴近——"7 天内"= 7 个格子；只算未完成，橙点才是纯粹的"该盯了"信号（与 D8 同一套判断基准，两个指标永远不会互相打架）。
- [ ] **B. [今天, 今天+7]**：今天起 7 整天（实际 8 个日历日）。**赌注**：窗口比字面多一天，说"7 天"其实是 8 天。
- [ ] **C. 窗口同上，但把已完成也算**。**赌注**：橙点从"紧急提示"变"近期活动记录"——你会在一堆已打勾的节点上看到橙点，提示失真。
- [√] 其他想法：

> （补充说明，可选）：实际工作中，仪表盘未来需要预留一个每周看板的功能；即，本周工作提示。我的意思是，我不关心【最近7天】，我只关心【本周】，也就是说，周一到周日，算作本周的工作范围。

*（详见草案 §5）*

---

## D12 · Excel 导入时，状态列跟日期"对不上"怎么办（⚠️ 推荐是 B，不是 A）

**这题在问什么**：你定过「状态只收三态文本，否则按行报错」。但"未开始/进行中"本来就是按日期算的——表里这两字可能跟日期矛盾。例：某行日期 8-10（已过去），状态列写「未开始」——按规则，日期过了又没标完成就是"进行中（逾期）"，跟表里写的对不上。「已完成」跟任何日期**都不矛盾**（它是人工判断，早标晚标都合法）。

> 假设：编辑器里状态永远按日期派生（已冻结）；导入不该引入第二套状态规则。

**【你的回答 — 选一个】**

- [ ] A. 拒绝对不上的行（行级报错）。**赌注**：你导出的旧表放两周（期间拖过几轮日期），状态文本全部过期——重导时一行行报错，且全是无害错误，纯烦。
- [√] **B. 日期优先，只认「已完成」（推荐）**——见到「已完成」就落库打勾（记打勾时刻）；「未开始/进行中」两字仅作参考、落库按日期重算；状态列空着 → 也按日期算；**不是三态文本的仍然按行报错**（之前拍的边界一点不松）。**推荐理由**：① "已完成"是表里唯一机器算不出来的信息（人工判断），必须收；② "未开始/进行中"从日期就能推出来，表里写它们本质是"抄结论"不是"新信息"，冲突时听日期的——**这样导进来的行为和你在编辑器里天天用的一模一样**（同一套规则）；③ 导入预览会明示"状态将按日期重算"，零惊吓。
- [ ] C. 三态全收、信表里的字。**赌注**：得给"未开始/进行中"也建人工覆盖存储 = 推翻你之前拍死的"状态由日期派生"，等于重开已冻结决策（按纪律仅列出供否决）。
- [ ] 其他想法：

> （补充说明，可选）：未来我会考虑开放一个【导入 md / json格式日期】的功能；即，导入可以是excel文件，也可以是md/json文件。到时候只需要提供标准模板（空白），就能先让 agent 清洗相关数据，再上传。这个不是难点

*（详见草案 §6）*

---

## D13 · 阶段条的起止日期：服务端算好给你，还是前端自己算（画法另定）

**这题在问什么**：数据库里只有一个个**单日节点**；仪表盘要画**连续的阶段条**（比如「开发」从 6-1 画到 7-15），就得知道每段从哪天到哪天。这题只拍 **API 给什么数据**——不决定画法（链式还是跨式好看，仍按约定我出 HTML 对照稿、你看了再定，本题不预支那个决策）。

> 假设：无论最终画法定哪边，编辑器永远以节点清单为唯一事实源；单项目/全项目两个仪表盘共用同一段计算逻辑。

**【你的回答 — 选一个】**

- [√] **A. 双份给：节点清单 + 服务端算好的区段清单（推荐）**——区段怎么算（链式/跨式）是服务端一个可切换的算法开关。**推荐理由**：① 以后画法改主意，只是换个开关，API 和前端一行不改——现在不用赌哪种画法；② 两个仪表盘用同一份区段计算，永远不会出现"单项目仪表盘和多项目仪表盘对重叠的判定不一样"；③ 单元②（保存后返回最新视图）和单元③都等这个数据结构，现在定下来后面不堵。
- [ ] B. 只给节点清单，区段前端自己算。**赌注**：同一段规则散到前端两处实现，改一处漏一处。
- [ ] C. 这题也等 HTML 对照稿一起定。**赌注**：数据结构悬着，单元②③开不了工。
- [ ] 其他想法：

> （补充说明，可选）：

*（详见草案 §7）*

---

## P1 · 六项高置信项打包确认（D3 / D5 / D6 / D9 / D11 / D14）

**这题在问什么**：以下六项 planner 置信度高、都有代码事实或你已拍的条款直接支撑，属于"实现层怎么落"而不是"产品语义怎么定"，逐题展开性价比低，打包请你过目——都是大白话版：

1. **D3 两个"出生证明"字段锁死**：谁建的项目（created_by）、节点首次录入的日期（initial_date）——任何正常接口都改不了：请求体里根本不收这两个字段、更新语句永远不含这两列、再配一条专门测试"任何路径都改不动"。不用数据库触发器（全库现在一个触发器都没有，不为这个破例）。
2. **D5 每次保存记一个"批次"**：谁、何时、以什么方式（编辑/拖拽/导入/撤销）改了哪些行——**直接改的行和被顺延带动的行分开记**（这样"改了 B 顺延了 C/D"就是一条批次里两种角色，复盘能分清人为改动和连带影响）；admin 的统一审计页通过一行桥接记录也能看到 timeline 操作，不用改造。
3. **D6 项目开始日期 = 所有节点里最早的那天**（含已完成、含并行轨一起算）；一个节点都没有的项目排在最后、显示「—」。
4. **D9 红点 = 只要存在"日期过了还没完成"的节点就亮**；两轨合起来数，不分开。
5. **D11 排序和提示用的数字每次查询现算、不存**：就 10 个项目、几百行节点，现算比"存下来+每次改完维护"又快又不容易出错（存了反而每次保存都要重算一遍缓存，纯亏）；间隔同理现算。
6. **D14 "今天"以服务器本地日（Asia/Shanghai）为准**，全部指标服务端算——避免谁的电脑时区或日期设置不同，看到不同的"今天"。

> 假设：以上均不触碰任何已冻结的产品语义。

**【你的回答 — 选一个】**

- [√] **A. 六项全部按推荐冻结（推荐）**。**推荐理由**：都是"怎么落地"的选择而非"产品要什么"的选择，且每项都有直接依据；一起过最省事。
- [ ] **B. 有异议**（请在补充说明写编号，如「D5：……」，单独展开讨论）。
- [ ] 其他想法：

> （补充说明，可选）：

*（详见草案 §3/§4/§5）*

---

## 1. 填答后的连锁动作（预告，不需操作）

- D1=A + D2=A → 草案 §2 表结构三件套转正为单元②/③ 的地基（v16 一次建齐字段，避免二次迁移）。
- D4=A / D7=A / D8=A / D10=A / D12=B → 草案 §3/§5/§6 对应节从【待拍板】改为「已拍板」，派生指标契约随之冻结为服务端唯一实现。
- D13=A → Claude 随后出「链式 vs 跨式」HTML 对照稿（唯一遗留的画法决策）；对照稿定案后单元①完全收口，进单元②（原子批量 API）问卷/草案。
- 全部收口 → 回填本文件 §0 确认记录，同步 mainline §8（未决项划销）、两级 STATE、Mc思考03 补一笔。
