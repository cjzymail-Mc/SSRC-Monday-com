# feature01 · 项目时间管理 + 仪表盘主线冻结基线

> 状态：用户于 2026-08-13 明确要求建立并锁定 feature01 主线。
> 2026-08-14 用户签字升级权限基线（§2/§5/§9）：纳入项目级创建者写权限（admin 全改 / member 仅改自己创建的），查看对所有注册成员开放；详见 `2-DRAG_TIMELINE_REQUIREMENTS.md` Q16–Q22。
> 2026-08-18 用户拍板（UX 问卷 Q1–Q8 + 样张遗留 R1–R3 全收口，见 `3-UX_DETAIL_REQUIREMENTS.md` §0）：§4.2/§4.3 行模型统一为「默认主线/并行两行 + 展开按钮，未展开时仅重叠段上下分半」，取代早期「单项目按重叠自动分轨」表述（PHASE2 §14.6 加覆盖注记）；§8 未决项大部分随之冻结；视觉方向 B 深色与右键菜单双模式拖拽等交互基线见 feature01 `STATE.md` §3。
> 2026-08-18 用户拍板（门 3 单元 ① 技术问卷 D1–D14 全收口，见 `archive/5-GATE3_UNIT1_DECISIONS.md` §0）：数据模型新增专表、派生指标契约冻结为服务端唯一实现；§4.3 橙点窗口由「7 天内」修订为「本周（周一~周日）」（见 §4.3 内覆盖注记）。
> **2026-08-19 用户覆盖拍板（视觉 F1）**：因 Flowboard 当前整体 UI 为浅色基调，feature01 v1 三页优先开发浅色版；单项目仪表盘以视觉样张**方向 A「大标题·白卡」**为当前执行基线，编辑器/全项目页沿用同一浅色 token 语言。2026-08-18 的方向 B 深色记录保留为历史，未来整体暗色皮肤须另行立项，不进 v1。
>
> 用途：约束 feature01 后续需求、原型、技术计划、开发与验收；防止把完整项目管理平台或视觉样图中的临时细节误当成必做范围。
>
> 当前阶段：业务规则、UX 与门 3 技术方案已收口；2026-08-19 F1 浅色覆盖增量复审 PASS，门 3 已重新关闭，数据/API/权限等其余 61 条未重开；技术上可进入门 4，但尚未激活正式编码 run。

## 1. 产品定位

在已交付的 Flowboard 之上，新增一套面向约 15 人鞋类研发团队的**项目时间节点管理与组合仪表盘**。

它首先解决三个问题：

1. 团队能像编辑 Excel 一样快速维护频繁变化的项目节点日期。
2. 单个项目能把六阶段、主线/并行节点和当前日期放在同一条绝对日历轴上理解。
3. 多个项目能在共享日历标尺下横向对齐，让用户快速判断当前阶段、临近节点和轻重缓急；延期历史按需进入轻量复盘。

本 feature 是 Flowboard 上的聚焦能力扩展，不另做独立产品，也不把 Flowboard 扩成完整的企业项目管理套件。

## 2. 目标用户与权限基线

- 主要用户：约 15 人鞋类研发团队，管理跑鞋、篮球鞋、羽毛球鞋等约 10 个并行项目。
- 复用 Flowboard workspace 角色：admin（当前 1 名）与 member（多名注册成员）；本团队暂不引入 viewer，不新建角色体系。
- 查看：单项目/全项目仪表盘与时间表编辑器对所有注册成员（admin + member）开放；未登录不可见；不引入访客/公开分享机制。
- 写权限（项目级创建者权限，2026-08-14 用户拍板纳入）：workspace admin 可修改/拖动所有项目时间轴；member 只能修改/拖动自己创建的项目时间轴（created_by = 自己）。
- 权限技术契约（2026-08-14 冻结，问卷 Q20–Q22）：created_by 由系统在首次创建时写入，此后任何人（含 admin）不可修改；initial_date 例外纠正权仅限 workspace admin（member 录错须找 admin，纠正留审计）；Excel 导入的项目 created_by = 导入操作者，第一版不做「指定创建者」功能。
- 项目实体入口（2026-08-18 用户 B5c=b）：admin/member 均可网页新建项目，created_by=当前操作者；仅 admin 可软删；v1 不提供项目改名或恢复入口。Excel 导入继续作为批量创建入口。
- 权限必须由服务端执行；只在前端隐藏按钮不构成授权控制。

## 3. 统一业务模型

### 3.1 项目生命周期

所有项目严格使用以下六个一级阶段，顺序固定：

```text
创意 → 设计 → 开发 → 测试 → 量产 → 应用迭代
```

本 feature 内不允许用户新增、删除、重命名或调整六阶段顺序。

### 3.2 时间节点

- 每个项目有独立的时间轴和不同的起止日期。
- 一行代表一个任务节点。
- 每个节点必须属于一个项目，并同时具备“项目阶段（一级标签）”和“项目节点（二级标签）”。
- 节点是单个日期上的里程碑，不是有起止日期的任务条。
- 必要业务字段为：`项目名称｜项目阶段｜轨道（主线/并行）｜项目节点｜日期｜时间间隔｜状态｜备注`。
- 主线与并行各自形成独立线性链，共用同一套节点/日期/间隔字段；两轨互不计算间隔、互不顺延。
- 将来可以通过受控变更增加字段，但不能破坏既有数据和导入导出兼容性。

### 3.3 日期、修改历史与轻量复盘

- 日常正式使用一个当前有效【日期】列，不使用计划日期/实际日期双列。
- 每次直接修改和系统级联顺延必须进入修改历史，并能区分直接编辑与自动影响。
- 用户始终只操作一个日期字段；新插入节点第一次保存时，系统无感写入该节点的 `initial_date`。
- 普通用户不能修改 `initial_date`；管理员可在首次录错时通过例外入口纠正，并留下审计。
- 不新增修改原因字段或强制弹窗；解释性信息继续写入既有【备注】列。
- 第一版不建设命名基线、定期快照或复杂版本系统；节点内部保留首次保存的 `initial_date` 与当前 `date`，并复用结构化活动记录中间修改。
- 日常仪表盘不默认展示历史延期天数；延期分析进入一个按需打开、可筛选单项目或多项目的轻量复盘页面。

## 4. 本轮必须交付

### 4.1 类 Excel 时间表编辑器

- 在网页内完成节点表的查看、增加、修改和删除。
- 行列结构明确，支持高频日期调整，降低现有 Flowboard 编辑时间节点的操作成本。
- 支持一个当前有效日期字段的快速编辑。
- 支持按需显示自动计算的时间间隔，并允许通过手改间隔触发受控的后续日期顺延；顺延一直影响到项目末节点，后续各段间隔保持不变，保存后提供一次撤销。
- 日期与间隔采用本次编辑字段优先级：只改谁就以谁为输入；两者同时修改时日期优先，并自动更正间隔。
- 系统内部间隔始终按日期时间链计算；编辑器支持“按日期（默认）/按六阶段分组”两种显示排序，切换排序不得改变日期、间隔或审计数据。
- 主线与并行各自维护独立日期时间链，共用同一【间隔】列；间隔与顺延只在本轨道内生效，两轨互不影响。
- 删除节点不移动其他日期，只重算合并后的相邻间隔。
- 提供清晰的保存、校验、错误反馈和只读状态。
- Excel 作为导入/导出格式；Google 文档不作为数据源或编辑器。

### 4.2 单项目仪表盘

- 展示一个项目的完整六阶段时间轴和全部节点。
- 使用绝对日历标尺和“今天”标记。
- 六阶段拥有稳定且可区分的专属颜色。
- 默认展示当前有效日期，不让普遍存在的历史延期标记淹没日常判断。
- 能识别当前所处阶段/附近节点和延期情况。
- 与该项目的节点清单表形成清晰、可直接编辑的组合体验。
- 轨道行模型（2026-08-18 拍板）：默认固定「主线/并行」两行，行内阶段重叠通过展开按钮拆分（展开后每阶段独占一行）；取代早期「按重叠自动分轨」方案。
- 未展开时，仅重叠段上下分半渲染，非重叠段保持满高；六阶段原色不混色、不遮挡（2026-08-18 拍板）。

### 4.3 全项目仪表盘

- 支持查看全部项目或多选部分项目。
- 每个项目一条泳道（泳道 = 项目容器，默认内含主线/并行两行，见本节末两条 2026-08-18 拍板），在共享的绝对日历标尺上横向对齐。
- 用相同的六阶段颜色语义表达各项目进展。
- 红色今日线贯穿所有泳道。
- 默认展示当前有效日期，不让普遍存在的历史延期标记淹没日常判断。
- 支持用户快速识别临近节点、延期项目、当前阶段和轻重缓急（2026-08-18 拍板）：顶部项目多选筛选 + 排序下拉（按开始日期 / 当前阶段先后 / 最近临近节点 / 逾期未完成节点数），行内只用克制小色点提示（橙 = 本周内有节点，红 = 有逾期未完成），不染整条泳道、不引入手动优先级字段。（⬆ 2026-08-18 门 3 问卷 D10 修订：橙点窗口由「7 天内有节点」改为「**本周（周一~周日，含今天所在自然周）内有未完成节点**，含本周已过去的天；红点不变；红橙可并存、红左橙右」——修订者为 Q5 原拍板人本人，覆盖注记见 `3-UX_DETAIL_REQUIREMENTS.md` §0 记录 5，细则见 `archive/5-GATE3_UNIT1_DECISIONS.md` §0 第 6 条。）
- 阶段业务顺序固定，但不同阶段日期允许重叠；仪表盘必须真实表达并行状态，不得通过修改日期或混合阶段颜色掩盖重叠。
- 每项目默认主线/并行两行（与单项目同一行模型），行内阶段重叠通过展开按钮拆分；未展开时仅重叠段上下分半（2026-08-18 拍板）；时间条统一使用 2px 小圆角矩形。

### 4.4 必要配套

- 与 Flowboard 现有登录、权限、活动/审计、备份恢复和部署方式一致。
- 数据迁移前备份且可恢复；不得损坏第一阶段已有数据。
- 覆盖数据层、API、可操作 UI、测试和文档。
- 使用真实或脱敏的代表性项目数据完成用户验收。

## 5. 明确不做

以下内容不进入 feature01 完成条件，除非用户以后明确调整本主线：

- Google Sheets / Google Docs 同步或协同编辑。
- 完整项目管理套件：OKR、工时、预算、资源负载、风险、审批和复杂工作流。
- 字段级权限（如某用户只能改日期不能改阶段）、部门级权限、项目级角色（如"项目A管理员"≠"项目B管理员"）、按对象实例单独授权的 ACL。（项目级创建者写权限已于 2026-08-14 由用户拍板纳入 §2。）
- 与排期无关的通用版本管理系统；本 feature 仍须保留支撑审计与延期复盘所需的日期变更历史。
- 任意自定义项目阶段、任意阶段顺序或通用工作流引擎。
- AI、CRM、Webhook、表单、SSO、原生 App 等第一阶段冻结主线已排除的产品线。
- 为本 feature 整体重写 Flowboard 或拆成新的独立系统。

## 6. 标准推进主线与阶段门

```text
产品主线冻结
      ↓
业务规则与关键用户旅程确认
      ↓
低成本 UX 原型验证
      ↓
视觉方向确认
      ↓
技术方案、数据与 API 契约
      ↓
纵向切片开发
      ↓
集成测试、视觉收口与真实数据用户验收
      ↓
备份、迁移、部署与运行收口
```

### 阶段门 1：业务规则可实现

进入技术设计前，至少确认：日期顺延规则、状态列语义、编辑器关键操作、仪表盘的判断任务和 Excel 输入输出边界。

### 阶段门 2：原型可用

进入正式编码前，用户应能通过静态原型完成代表性任务，并确认页面结构、交互方式和主要视觉方向。此时确认“避免胶囊化”等方向，不必过早锁死具体圆角、色值和字号像素。

### 阶段门 3：技术方案安全

编码前完成数据模型、API、服务端权限、审计、并发、迁移、备份恢复和旧功能回归策略评审。

### 阶段门 4：纵向切片验收

每个开发检查点必须同时包含数据层、API、可操作 UI、测试和文档。单独完成静态页面、数据库或接口均不算检查点完成。

### 阶段门 5：可上线

真实数据关键旅程、权限、导入导出、日期计算、两类仪表盘、最终视觉人工验收、迁移恢复和第一阶段回归全部通过，才允许进入部署收口。

## 7. UI / UX 决策原则

UI 讨论贯穿全过程，但关注点分层：

1. 原型阶段先确认信息层级、编辑效率、操作反馈和防错。
2. 交互稳定后确认颜色体系、圆角倾向、密度和信息层级等视觉方向。
3. 集成和用户验收阶段基于真实数据集中完成具体色值、圆角、字号、行高、间距和响应式的最终美化收口，不无依据推翻核心交互。

当前静态样图只是验证工具，不是最终视觉规范。条形圆角已冻结为 2px 小圆角（2026-08-18）；剩余像素项（精确色值、条形粗细、字号、行高、留白）按本节第 3 条留集成验收期基于真实数据收口。

## 8. 尚未冻结的设计细节

以下问题必须继续讨论，但不阻止本产品主线成立：

> 2026-08-18 更新：轨道控件、同日排序、状态枚举、Excel 契约、全项目筛选排序、行模型与重叠渲染均已冻结（见 `3-UX_DETAIL_REQUIREMENTS.md` §0）；以下仅剩视觉像素与纯技术项。

- 六阶段的最终精确色值（色相方向已冻结，v3.2 为当前执行色板；真实数据微调留集成验收）。
- 条形粗细、节点点样式、字号、行高和留白（圆角已定 2px；磁吸强档与放大带参数微调留集成验收）。
- ~~视觉主题范围~~（**2026-08-19 用户覆盖拍板：feature01 v1 三页优先浅色，单项目以方向 A「大标题·白卡」为执行基线；只保留语义 token，不实现主题切换或暗色运行态。未来 Flowboard 整体暗色皮肤另行立项。** 2026-08-18 F1=b 深色结论仅作历史追溯，当前以 `9-GATE3_TECH_FREEZE.md` §8/§11 为准）。
- ~~单项目仪表盘「链式 vs 跨式」画法对照~~（**已拍板 2026-08-18：A 链式**——相邻节点连线成条、段色=左端节点阶段，忠实呈现交错；D13 原始双份契约经门 3 安全补全后，API 恒返 nodes + segments + stage_intervals 三份，段算法仍可切换；重叠/展开由服务端单一实现。见 `（画法对照）stage-bar-chain-vs-span.html` 拍板区与 `9-GATE3_TECH_FREEZE.md` §2.4）。
- ~~Flowboard 现有表的复用方式或新增项目/阶段/节点表~~（**已拍板 2026-08-18：D1=A 新增专表** timeline_projects/nodes + 批次审计两表，v16 纯增量；间隔/状态不落库实时派生。见 `archive/5-GATE3_UNIT1_DECISIONS.md` §0）。
- ~~多人并发编辑的冲突提示与解决方式~~（产品语义已冻结于 `2-DRAG_TIMELINE_REQUIREMENTS.md` 确认 5 + 问卷 Q10；**技术实现已拍板 2026-08-18：E1–E8**——单端点单事务整批全成全败、逐项目 base_version 409 带最新视图、undo 护栏四件套。见 `archive/7-GATE3_UNIT2_DECISIONS.md` §0；安全修订以 `9-GATE3_TECH_FREEZE.md` 为准）。

这些内容在确认后进入需求验收标准、UX 规范或技术方案；不得只存在于口头上下文。

## 9. 完成边界

feature01 只有在以下结果同时成立时才算完成：

- 用户能以接近 Excel 的效率维护项目节点，无须再靠 Google 文档解决协作编辑。
- 六阶段、轨道、节点、日期和间隔在编辑器及两类仪表盘中共享同一份数据和业务规则。
- 单项目仪表盘能回答“这个项目现在到哪里、下一节点是什么、是否延期”。
- 全项目仪表盘能回答“所有/所选项目今天分别在哪里、谁延期、谁临近关键节点、谁更紧急”。
- workspace admin 全项目写权限与 member 创建者写权限均由服务端正确执行；仪表盘/编辑器对所有注册成员可见、未登录不可见。
- Excel 导入导出可承载约定数据且错误可解释。
- 数据迁移、备份恢复、审计、测试、文档和第一阶段回归均通过。
- 本文件“明确不做”的内容没有被擅自纳入。

静态样图通过、某个页面做完或某个接口通过，均不等于整条 feature01 主线完成。

## 10. 纠偏检查

每次制定计划、调整原型、开始检查点或准备验收时回答：

1. 当前工作是否直接提高节点编辑效率或项目时间判断效率？
2. 是否让编辑器、单项目和全项目仪表盘共享同一数据与规则？
3. 是否把尚未确认的样图细节误写成冻结需求？
4. 是否把完整项目管理或其他未来功能偷偷纳入 feature01？
5. 是否省略服务端权限、审计、迁移、备份、恢复或测试？
6. 是否保护第一阶段既有功能和旧数据？
7. 当前成果是否有可操作证据和明确验收标准？

任一答案表明偏离时，先修订需求、原型或计划，再继续。

## 11. 文档优先级

feature01 内发生冲突时按以下顺序处理：

1. 用户最新明确指令。
2. 本文件的产品范围、工程原则和完成边界。
3. `1-PHASE2_REQUIREMENTS_DRAFT.md` 中用户已明确回答的业务事实。
4. feature01 的当前 `STATE.md`。
5. 后续 UX 规范、技术方案和实施计划。
6. `mockup.html` 等探索性样图。

样图用于提问和验证，不能自行覆盖主线。调整本文件冻结范围须由用户明确拍板。
