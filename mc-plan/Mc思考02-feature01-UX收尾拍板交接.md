# Mc思考02 · feature01 UX 收尾拍板交接（/session-new 交接物）

> 建立：2026-08-14 · 会话交接用一次性文档（不进 memory）
> 本 plan 服务的主线：`STATE.md` 一句话当前状态——feature01 需求与 UX 原型收敛期，**当前卡在等用户填答问卷 Q1–Q8**。

## 目标

新会话接手后：读回用户在 `3-UX_DETAIL_REQUIREMENTS.md` 的 Q1–Q8 勾选 → 按问卷模式评估（找矛盾/空白/连锁影响）→ 回填 §0 确认记录 → 同步 mainline / STATE → 进入 mainline §6 阶段门 3（技术方案）。

## 执行环境

- repo：`D:\Downloads\Mc Claude Code Workshop\monday-com`（plan 存放 repo = 执行 repo，同一处）
- 开工必读（按序）：
  1. `feature-01-项目时间管理-仪表盘/STATE.md`（feature 冷启动入口，已同步到 2026-08-14 最新）
  2. `feature-01-项目时间管理-仪表盘/3-UX_DETAIL_REQUIREMENTS.md`（**当前活跃**，看 §0 速览表 + 用户勾选）
  3. `AGENTS.md` §2 问卷往返模式（评估/回填纪律）

## 关键决策（已拍板，勿走回头路）

- **DRAG Q1–Q22 已全部收口**（拖动专题 + 权限模型 + 三条技术契约），mainline §2/§5/§9 已同步落盘——不要再重开这些题。
- **权限基线**：admin 全改 / member 仅改自己创建的项目（created_by）；查看对所有注册成员开放、未登录不可见；created_by 创建后永不可改（含 admin）；initial_date 纠正仅限 admin；Excel 导入 created_by=导入操作者。
- **状态列三态**：未开始 / 进行中 / 已完成（DRAG Q17），不单开完成日期列。
- **mock 演示推荐项不算代签**（mc-expert 陪审高置信度），三条件：①演示标注"待拍板"②备选项仍可看③改动聚块可回滚——记录在 feature01 STATE.md §6 过程注记。
- **排除掉的方案**：PHASE2 草稿中「计划/实际双列」「注册用户全权」「非注册用户只读」「基线快照系统」均已被后续拍板取代（草稿已加 ⬆ 注记，以注记为准）。
- **Q7 背景**：用户主动提出"泳道=轨道（主线恒第 1 行、并行第 2 行）+ 展开按钮"，调整的是 PHASE2 §14.6"单项目无限自动分轨"（那是轨道列出现前的方案）——用户拍板后需改写 mainline §4.2/§4.3 + 给 §14.6 加取代注记。

## 前提状态 + 快速核验

- 问卷 Q1–Q8 已出题（8 题含完整选项/赌注），§0 有拍板速览表 → 打开文件看 §0 是否已有用户勾选。
- `mockup.html` v3.1：Q2（轨道下拉）/ Q5（排序+色点）/ Q6（Excel 预览）/ Q7（两行模型，并行轨只读）推荐演示已就位，均标注待拍板 → 浏览器打开验证可交互；banner 有版本说明。
- `dual-track-lane-options.html`：Q7 展开↔多行 + Q8 三方案对照 → 打开可点展开按钮。
- 两个 HTML 内联 JS 已过 `node --check`。
- git 工作区有大量未提交改动（本阶段全部产物未落库）。

## 实施步骤（主线下一步，未落其他 plan，以此为准）

1. 读 `3-UX_DETAIL_REQUIREMENTS.md` 各题勾选（用户若只填了部分：已填的先评估回填，未填的继续等，不代填）。
2. 评估重点：Q7 选 A/C → 改写 mainline §4.2/§4.3、PHASE2 §14.6 加 ⬆ 取代注记；Q8 选 C → 在 mainline §4.3 显式记录"允许默认视图遮盖重叠"的红线放宽；Q5 选 C → 牵范围，须再确认。
3. 回填问卷 §0 确认记录（沿用 DRAG 文件 §0 的编号格式，从 25. 起或重新计数均可，注明日期）。
4. 同步 feature01 `STATE.md`（§2/§6/§7）与根 `STATE.md`（变更日志 +1 行）；`dual-track-lane-options.html` 降级留档说明。
5. Q8 连带项（见问卷 §0 末注记）：Q7 拍板后给"链式条形 vs 阶段跨条"两种单项目画法对照，让用户再定一次（轻量，可并入技术方案前）。
6. Q1–Q8 全收口 → 需求侧无已知未决项，进 mainline §6 阶段门 3：技术方案（数据模型 / API / 服务端权限 / 审计 / 并发 / 迁移 / Excel 契约细化）。届时改用 planner↔coder 模式（AGENTS.md §2 末条：问卷模式仅限调研对齐阶段）。

## 待拍板决策点（带建议默认）

- 各题推荐选项见问卷 §0 速览表；建议默认 = 表中推荐列（A/A/A/A/A/A/A）。
- KB repo 候选卡 `proposal-repo-map-add-monday-com.md`（文案已拟好）因写入分类器故障未落盘——若用户未处理，新会话可补写（目标：`D:\Downloads\Mc Claude Code Workshop\Mc-Expert-repo\.claude\auto-memory\`）。

## 边界 / 红线

- 改冻结 mainline 须用户显式拍板，不代签；问卷评估发现与主线冲突必须显式 ⚠️ 并要求拍板。
- 不进编码、不碰主程序与数据库；像素级视觉（色值 hex/字号/行宽）留集成验收收口。
- 未经用户显式发起不做 git commit；不代改 AGENTS.md（治理文件）。

## 成功标准

Q1–Q8 全部有用户勾选 → §0 回填完整 → mainline/STATE 同步自洽 → 需求侧无未决项，具备进阶段门 3 的条件。
