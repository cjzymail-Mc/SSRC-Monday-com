# Mc思考03 · feature01 需求收口后 codex 只读纠偏 + 阶段门 3 启动

> 建立：2026-08-18 · 阶段决策记录（凝固态，陈述当时事实，勿回溯改写）
> 评估原文：根目录 `temp.md`（codex 只读系统评估，未改任何文件/代码/数据库；用户已同步追加快照到 `Mc思考01` 末尾留档）。
> 本 plan 服务的主线：feature01 已过 mainline §6 阶段门 2（需求 + UX 全收口），本记录固化这次外部纠偏的结论与处置，并作为门 3（技术方案、数据与 API 契约）的启动依据。

## 1. 纠偏事件

2026-08-18，用户以 codex 对 feature01 主线做了一次**只读系统评估**——相当于 mainline §10 纠偏检查清单的一次外部执行。评估未动任何文件、代码或数据库。

## 2. 评估结论 · 被确认的部分

- 主线清晰可执行、**范围无漂移**；当前位置 = **门 2 已过、门 3 未完成**；阶段门不等权，不能换算成"工程量完成 40%"。
- 需求与 UX 已收口（Q1–Q22 + Q1–Q8 + R1–R3）；正式代码、数据库迁移、API、测试均未开始——与根/feature 两级 STATE 记录一致，无状态虚报。
- 主线质量点被确认：产品定位/必做/不做/完成边界清晰；编辑器与两类仪表盘共享同一数据模型；服务端权限、审计、软删除、迁移前备份、旧功能回归均为硬条件；纵向切片检查点五件套（数据层+API+可操作 UI+测试+文档）防假进度。
- 底座可复用（schema 版本号、软删除、迁移、审计机制已有），但 boards/tasks 现无 feature01 所需的 `created_by` 与节点初始日期模型（`flowboard/database.py` 160/187 附近）→ **现在进门 3 是正确节奏**。

## 3. 评估抓到的缺口 · 本次处置

| # | 缺口 | 处置 |
|---|------|------|
| 1 | **派生指标算法未写死**：全项目排序/提示所用的「项目开始日期、当前阶段、最近临近节点、逾期未完成节点数、7 天内节点」，在阶段重叠、双轨并行时如何计算没有正式契约 | **新增门 3 议题**（feature01 `STATE.md` §7 已补第 8 条），进「领域对象与派生规则」首个决策单元 |
| 2 | **状态与 Excel 导入语义缺口**：状态 = 日期派生（未开始/进行中）+ 人工（已完成），但 Excel 允许导入三态文本；矛盾时拒绝/覆盖/重算未定 | **新增门 3 议题**（feature01 `STATE.md` §7 已补第 9 条，并入 Excel 契约细化），planner 出选项、用户拍板 |
| 3 | **阶段条生成规则未冻结**：源数据只有单日节点，仪表盘要连续阶段条；「链式 vs 跨式」未决，直接影响查询结果与前端数据结构 | 已在门 3 议程（原 §7 第 6 条）；按评估确认其数据结构影响，**升入首个决策单元一并处理**（画法对照稿仍按 mainline §8 安排出给用户再定一次） |
| 4 | **文档漂移**：①「一项目一条泳道」vs「每项目默认两行」术语不一；② mainline 残留「下一版必须降低圆角」旧表述（圆角已冻结 2px）；③ 数字前缀改名后数处文件引用失效 | **本次当场清理**（见 §4；仅术语注记/过时表述/失效引用，不改任何已冻结决策） |

## 4. 本次已执行的文档清理（2026-08-18，随本记录一并落盘）

- `mainline-feature01.md`：§4.3「每个项目一条泳道」补注（泳道 = 项目容器，内含主线/并行两行）；§7 删除过时的「下一版必须降低圆角」句，改为圆角已冻结 2px、剩余像素项留集成验收；头部与 §11 共 2 处旧文件名引用改为数字前缀新名。
- feature01 `STATE.md`：§7 议题清单补第 8/9 条（派生指标契约、Excel 导入状态语义，注明纠偏来源）；2 处旧文件名引用修正。
- `1-PHASE2_REQUIREMENTS_DRAFT.md`：头部 1 处旧引用修正。
- 根 `STATE.md`：变更日志 +1 行；feature01「下一步」同步。
- `mc-plan/` 历史文件（Mc思考01/02）为凝固态记录，其中的旧文件名引用**不改**（当时事实）。

## 5. 门 3 推进顺序（采纳 codex 建议，经用户「继续推进」指令确认）

纪律：一次只聚焦一个最小可决策单元，不开平行多坑。

1. **单元 ① 领域对象与派生规则**：数据模型落位（复用 boards/tasks vs 新增专表）、`created_by`/`initial_date` 落位、审计批次表、派生指标契约（缺口 1）、链式 vs 跨式的数据结构影响（缺口 3）、Excel 状态语义选项（缺口 2）。planner 起草 → 用户拍板。
2. **单元 ② 原子批量 API**：草稿批次一次提交、并发版本号、整批全成全败、撤销批次、服务端权限执行。
3. **单元 ③ 迁移与收口**：schema 迁移 + 备份恢复 + 第一阶段回归策略 + Excel 解析器契约细化 + 深色主题范围。
4. 三单元全部过用户评审 → 过门 3 → 进纵向切片（门 4）。

**模式切换**：问卷往返模式结束（AGENTS.md §2 末条，仅限调研对齐阶段）；门 3 起 planner↔coder 模式——planner 起草技术方案、重大取舍交 mc-expert 挑战 + 用户拍板，coder 只在方案冻结后动代码。

## 6. 待拍板决策点（首轮，随 planner 草案逐项过）

- 数据模型落位：复用现有表 vs 新增专表（planner 给建议 + 赌注）。
- 派生指标契约：阶段重叠/双轨并行时「当前阶段」「项目开始日期」等取值规则。
- 链式 vs 跨式：Claude 出对照样稿后用户再定（mainline §8 既定安排，不影响已拍板的重叠渲染原则）。
- Excel 导入状态矛盾：拒绝该行 / 覆盖重算 / 信任导入文本（三选项带赌注）。
- 深色主题范围：仅仪表盘 vs 全站跟随（成本问题，可留到单元 ③ 再拍）。

## 7. 边界 / 红线

- 门 3 期间**不编码**、不碰主程序与运行库（`flowboard.db` 只读优先）。
- 改冻结 mainline 的范围/决策须用户显式拍板；本次清理仅限术语注记、过时表述与失效引用，不构成决策变更。
- 未经用户显式发起不做 git commit；不代改 AGENTS.md（治理文件）。

## 8. 成功标准

门 3 三单元全部成文并过用户评审；§6 决策点全部有拍板记录；具备进纵向切片（门 4）的条件。

---

## 9. 补记：单元 ① 拍板结果（2026-08-18 同日）

技术问卷 `5-GATE3_UNIT1_DECISIONS.md` D1–D14 全部拍板（§0 有确认记录）：D1=A 新增专表、D2=A 项目独立实体、D4=A done_at（含「不算新增完成日期列」认定）、D7/D8=A 派生指标取法、**D10 用户修订橙点窗口为「本周（周一~周日）」**（覆盖原 Q5「7 天内」，mainline §4.3 / 3-UX §0 记录 5 已加覆盖注记）、D12=B 日期优先只认已完成、D13=A 节点+区段双份契约（段算法可切换）、P1=A 六项打包冻结（D3/D5/D6/D9/D11/D14）。**画法拍板（同日）= A 链式**（未随 planner 倾向 C；知情接受空档被连线盖住，配套推导=服务端在链式段外同派生「阶段区间列表」供重叠分半/展开拆行，服务端单一实现不变）。两个未来候选记录在案不进 v1：每周看板（本周工作提示，以本周窗口为语义地基）、md/json 导入（空白模板 + agent 清洗）。**单元 ① 至此完全收口，进单元 ②。**技术拍板沿用问卷形式 = 用户 2026-08-18 指令。

**单元 ② 补记（同日）**：`7-GATE3_UNIT2_DECISIONS.md` E1–E8 全部拍板（单端点单事务 / base_version 整批 409 带最新视图 / 撤销护栏四件套 + 写权限可撤 + 跨项目 undo_group 等价 / 空批 422·全等值 no-op / **E7c 混合批次拍 a 行级区分** / 钉住+分段平移 / P2 七项打包含意图提交服务端重算）——§5 单元 ② 收口。同日治理升级：**AGENTS.md §2 新增 mc-expert 陪审先行规则**（用户授权：陪审高置信非体验项定稿，无法决定/不确定/重大体验影响→【需人工拍板】），单元 ③ 首跑；门 3 文档组织拍板 A+B（单元 ③ 起单文件出题，门 3 收口总固化《技术方案冻结版》+ 过程稿归档 `archive/`）。

**单元 ③ 补记（同日）——门 3 三单元齐**：单文件问卷 `8-GATE3_UNIT3_DECISIONS.md`（A+B 首产物：通俗正文+附录实现规格 B1–B5）。陪审先行首跑：planner 素材全部代码断言经 mc-expert 独立复核相符（含 158 处硬编码色值、E2E 无像素断言、backups 13 快照）；14 题中 12 题陪审定稿（F2 解析器细则/F3 迁移 runbook 含 CLI 备份强制与五表一次建齐/F4 回归清单/F5 GET 两端点全量不分页/P3 八项打包），仅 F1（体验）与 F2-④（使用习惯）升级人工。用户拍板：**F1=b** feature01 三页整体深色（看板维持亮色）；**F2-④=b 项目名称唯一、不允许同名**——未采纳陪审默认落点 a，产生联动修订 R1–R5（同名报错有错不许提交/防重跳过失效/预览要素修订/**R4 v16 唯一约束改部分唯一索引**（原 UNIQUE 因 SQLite NULL 语义不约束活跃行）/导出往返仅新 workspace 场景），`4-` 头部已加修正注记。F2-②=b 附用户工作流注：标准 Excel 模板 + agent 预清洗。陪审另出三条规格补丁。下一步：门 3 总固化（蒸馏《技术方案冻结版》）→ 门 3 整体评审 → 门 4。
